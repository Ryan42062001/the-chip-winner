# TCW-049 — Repaired Exact-SHA Independent Workflow / Security Re-Audit

Date: 2026-09-19 (America/New_York)
Role: Independent Auditor / QA — fresh, separate workflow/security re-audit lane
Workflow: canonical V3.2 · STANDARD_CHAT_HIGH · FAST_REFRESH

**INDEPENDENT VERDICT: PASS — for this exact pre-installation implementation/security audit scope.**

This PASS is not the historical TCW-048 verdict, not an automatic freeze/merge/installation authorization and **not** evidence that the new first-party master-push/dispatch workflow has executed on the default branch. Its separately controlled post-installation Actions exercise remains mandatory.

## 1. Independent first-gate and custody verification

- Live `master` independently compared IDENTICAL to canonical activation integration commit `d2fbba6518417827e2f3d6bf4d6422b415c519c1`. Dedicated Auditor branch `auditor/tcw-049-readiness-workflow-security-reaudit` also compared IDENTICAL to that master before evidence creation. Actual HISTORICAL branch-creation baseline remains `93436f250bd38bf97357c342b84a59a02adc28fc`; branch was advanced non-force. Historical-baseline→current Auditor branch was 10 commits ahead, zero behind, consisting of Manager activation/control-plane and temporary original-helper-workflow removal; historical creation SHA is not rewritten.
- Canonical `.ai/manager/tasks/TCW-049.md`, `.ai/audit/TCW-049_WORKFLOW_SECURITY_REAUDIT_PACKET_7c5bd1ad.md`, and active registry independently match: separately numbered TCW-049, owner Auditor, role Independent Auditor / QA, ASSIGNED, exact Builder target `7c5bd1add860d1e8ed7bc03717451c7d88a21c50`, Builder PR #162, original Auditor baseline and dedicated branch. Only two Auditor evidence paths are permitted: THIS report and `.ai/auditor/TCW-049_HANDOFF.md`. No TCW-046/048 evidence overlap.
- Builder PR #162 independently verified OPEN/DRAFT/UNMERGED, branch `builder/tcw-047-automated-audit-readiness`, exact immutable HEAD `7c5bd1add860d1e8ed7bc03717451c7d88a21c50`. Historical authorized Builder creation baseline `7ca2953009d37a014e041cc24f4934bfe61b5cad`; historical frozen FAILED TCW-048 target/repair parent `acb63b0c85b98b34fac9af99f00f38553de5670c` remains unchanged. GitHub baseline→repaired target independently compared 19 ahead/0 behind with exactly four authorized files: `.github/workflows/task-audit-readiness.yml`, `scripts/workflow-audit-readiness-automation.js`, `test/workflow-audit-readiness-automation.test.js`, `.ai/builder/TCW-047_HANDOFF.md`. Failed-target→repaired-target: 5 ahead, only runner/test/handoff changed; workflow YAML unchanged. No original-helper/static/package/product/field-validation/TCW-034/035 edits.
- Independently read Workflow V3.2, task TCW-047, TCW-049, frozen TCW-049 packet, Manager new freeze, TCW-048 finding disposition, historical immutable TCW-048 audit report retrieved from its original Auditor commit `84ec5e38ac6b027f77c40e6cc47df6f4f8165fb1` (historical evidence is not present at that path on current master), all four exact repaired Builder files, the actual original mechanical helper and static validator, and package.json. No previous author/Builder/Manager verdict is adopted as this verdict.

## 2. Independent verification of supporting CI and original mechanical preflight

| Supporting evidence | Exact checked SHA / run | Independent result |
| --- | --- | --- |
| Final combined code, tests AND handoff FULL Builder CI | `7c5bd1add860d1e8ed7bc03717451c7d88a21c50`, workflow #723 / run `35484883666` / test job `106009319646` | SUCCESS; exact HEAD and full unit/contract step verified; not independent security proof |
| Separate ORIGINAL task-specific mechanical preflight | Manager triggering SHA `93436f250bd38bf97357c342b84a59a02adc28fc`; independent original-helper workflow run `35485696728`, job `106011526029` | SUCCESS; independently inspected live job steps and logs: explicit Builder exact checkout, assigned branch and baseline, canonical Manager overlay, PR head checks before and after, original packet checker. Logged packet SHA256 EXACT `74277fd077fd47e117b2071710b0d3fb6667c27bf85c007b5530d39f26794182`, readyForManagerFreeze true, blockers []. This is the one-time ORIGINAL helper only, NOT the new automation. |
| Current canonical activation master push | `d2fbba6518417827e2f3d6bf4d6422b415c519c1`, workflow #730 / run `35486039468` | SUCCESS; test job `106012486063`, deploy `106012674830`, verify-production `106012710106` all SUCCESS. This existing Deploy website workflow is NOT the new task readiness workflow. |

Independently retrieved the original script/validator/package from the frozen trust-anchor commit `86a7f95217e6152db397ada8039533a7f4722b3a`, current activation master, and repaired Builder target. All three copies have identical exact bytes and Git blob IDs: `scripts/audit-workflow.js` `37ba581f504b1d6864230667f68191c3f22371a9`; `scripts/workflow-audit-readiness.js` `b628b3f6a334ad36e5f71235b78d88dd35f7d9e6`; `package.json` `fa639631d3258f501858edf55b3ed6735e405998`. No later handoff-only SHA replaced the full-tested target.

## 3. TCW-048-F01 — CLOSED within independently tested design/implementation bounds

Prior HIGH/BLOCKING defect: original Builder checkout could own/replace the mechanical helper, static validator or npm entry point and generate a self-hashed PASS packet even when future Manager-authorized scope included those paths.

Repaired exact-source chain:
- `scripts/workflow-audit-readiness-automation.js` lines 15–22 pin trust-anchor commit and enumerate both original scripts plus complete `package.json`. Lines 57–77 read pinned trust bytes via exact `git show SHA:path`; separately require byte-identical canonical Manager and Builder checkout files. This does not accept a moving Manager branch/Builder-provided trust list.
- Lines 178–191 strip GitHub/Actions runtime tokens, Git HTTP config and `NODE_OPTIONS`/`NODE_PATH`/npm environment overrides before helper execution. Lines 247–284 suppress npm pre/post lifecycle hooks (`--ignore-scripts`), require authenticated code again, directly execute the original helper using runner `process.execPath`, validate both packets and require byte-for-byte JSON agreement; lines 330–340 require trust check before and after helper execution.
- Independently constructed source-extracted verifier attestation harness (mock file/anchor IO): clean canonical/Builder three-file bytes PASS; mutating **each** Builder original-helper/static/package file independently REJECTED; canonical Manager helper changed without explicit trust-anchor update REJECTED. Independently source-extracted `runMechanical` with mocked spawn outcomes: a forged/different npm changed-files packet was REJECTED by direct-original-packet comparison; identical original vs npm packet PASS, and the two invocations were exactly `npm --ignore-scripts --userconfig=/dev/null run --silent workflow:audit-readiness -- --task TCW-101` and `/trusted/node scripts/workflow-audit-readiness.js --task TCW-101`.
- Frozen original SHA256 verifies packet integrity, **not source authenticity by itself**. Protection arises from the separately pinned code-byte attestation and direct matching trusted-helper execution. Real tracked file bytes were independently compared as above. Builder's separate synthetic full Git fixture is supportive CI, NOT substituted for these independent challenges.

Conclusion: historical self-certified-helper PASS route no longer reproduced on this exact target. An intentional future upgrade of any trust-anchor file must undergo a separately reviewed update; the currently frozen anchor may not be silently rederived. No malicious real PR or untrusted process was executed against GitHub in this Auditor lane.

## 4. TCW-048-F02 — CLOSED within independently tested design/implementation bounds

Prior MEDIUM/BLOCKING defect: malformed canonical state could yield successful `NO_ELIGIBLE_TASK` without any original static validation because the loop was empty.

Repaired source: `validateCanonicalState` lines 79–90 first authenticates original canonical validator, invokes it directly via runner Node against canonical Manager checkout and FAILS on nonzero status. `main` lines 385–416 performs this on **every invocation before parsing task selection**, whether master push or manual dispatch; lines 423–435 write a global FAIL/INFRA_ERROR record and summary. The previous `selectEligibleTasks` root/ID validation and summary's valid `NO_ELIGIBLE_TASK` behavior remain but are no longer the only integrity gate.

Independent exact-source controlled harness (mock process result, explicit source entrypoint): original canonical checker with zero eligible tasks and exit 0 PASS; same checker reporting `ERROR ... merge_authority must be Manager` exited 1 and produced classification FAIL with diagnostic; EACCES/unavailable canonical static checker returned INFRA_ERROR, not a false NO_ELIGIBLE_TASK. This explicitly challenges an *inactive* malformed task, outside Builder's usual positive fixture. Independently reviewed real original `scripts/audit-workflow.js` validation of registry shape/task specs, and read Builder's added on-disk synthetic end-to-end cases for invalid root `active_only`, `workflow_overlay`, inactive task authority and status-spec mismatch, including artifact assertions. Builder fixture outcomes are supporting evidence; independent mock is narrower than genuine checkout execution.

Conclusion: invalid Manager static state no longer silently receives NO_ELIGIBLE_TASK; valid no-work still intentionally gets that distinct status. A live master-triggered zero-eligible trial remains the post-installation gate, not claimed here.

## 5. TCW-048-F03 — CLOSED within independently tested design/implementation bounds

Prior LOW/same-pass defect: transient local infra exceptions could be reported as Builder FAIL, and static helper stderr was discarded.

Repaired source: lines 34–39 explicitly classify a local Git execution failure as INFRA_ERROR; lines 44–55 sanitize raw/token and encoded authorization header, Bearer/Authorization strings and nonprinting control chars; lines 79–88 and 247–261 preserve bounded sanitized canonical static/helper stdout+stderr on rejection; lines 357–365 and 423–435 distinguish FAIL vs INFRA_ERROR and write per-task/global JSON/diagnostic artifacts and summary. A known environmental error's `code` maps to INFRA_ERROR, but deliberate scope/trust/authority violations retain FAIL.

Independent exact-source controlled adversaries: ENOSPC, EACCES and explicitly classified Git transport errors => INFRA_ERROR; an explicit trust/scope failure and unclassified logical exception => FAIL. A synthetic error containing raw fake token, fake Base64-auth header, Bearer credential and ghp_ prefix was fully redacted (no raw token/header remained). The independently exercised canonical-validation error path yielded FAIL for corrupt task authority and INFRA_ERROR for EACCES. These are function/mock tests; no real disk failure or live credential was induced. Artifact upload `if:always()` retains **extant** evidence for normal negative paths; catastrophic runner/filesystem failure before evidence creation cannot guarantee a durable artifact and is not claimed.

Conclusion: historical F03 deficiencies no longer reproduced in tested code paths; no new blocking regression identified.

## 6. Preserved independent workflow and read-only controls

- YAML `.github/workflows/task-audit-readiness.yml` lines 3–64 (unchanged during repair) selects canonical `master` push upon Manager registry/task changes or explicitly keyed `workflow_dispatch`; grants only `contents:read`/`pull-requests:read`, checkout pinned to `github.sha` at full history with `persist-credentials:false`, quoted arguments via environment variables, Node 22, and `if:always()` retained 30-day artifact. No PR-origin execution event, `pull_request_target`, write token or auto-merge/freeze step. Runner independently rejects noncanonical `GITHUB_REF`, unexpected repository or event (lines 385–390).
- Runner validates syntactic task/branch/SHA, Manager/Builder ownership, exact remote assigned branch HEAD and open PR number/base/branch/head/repository (lines 96–157 and 311–327), fetches only the assigned branch then requires FETCH_HEAD to equal immutable checkpoint; isolates that exact commit under assigned branch name and checks assignment-baseline ancestry and changed paths with `git diff --no-renames` to include old/new paths (lines 206–245, 318–329). Canonical `.ai` overlay and clean post-overlay checkout checks remain; no Builder commits are modified. Remote Builder branch/PR is rechecked after helper execution (lines 343–353); later drift at Manager freeze is separately rechecked by Manager.
- Independent source-extracted remote/race adversaries: matching branch+PR yields zero blockers; moved branch, moved PR SHA and foreign PR repository each generate a blocker. Independent two-task push selection and aggregation: both selected, all PASS -> PASS, omitted task -> INFRA_ERROR, one FAIL -> FAIL; valid zero count -> NO_ELIGIBLE_TASK. Only selected task IDs, not arbitrary caller SHA, control dispatch. Scope checks and packet SHA/provenance are augmented by original trusted-helper checks, not bypassed.
- Runner output remains `MECHANICALLY_READY_ONLY`, never an Auditor verdict or permission to freeze, merge, approve trade valuation, send ESPN writes, or activate TCW-035. There is no separate `git push`/`merge` or GitHub write API action in the scoped files. `.github/workflows/task-audit-readiness.yml` remains ABSENT from canonical master before Builder integration. The removed one-off original mechanical preflight was not the proposed automated gate.

## 7. Validation levels, limitations and mandatory future gate

| Level | Independent outcome |
| --- | --- |
| L1 independent exact-source/diff/registry/role review | PASS — original F01/F02/F03 defect paths addressed; preserved controls reviewed |
| L2 exact immutable Builder FULL, original separate mechanical packet and canonical activation master CI | Independently verified SUCCESS at their stated exact SHAs/runs; CI is supporting evidence, NOT proof of security correctness |
| L3 independently executed additional source-extracted controlled attack/mutation/identity/aggregation/diagnostic harnesses | PASS within bounded function/mocked process/file IO scope; no full local npm/browser, real Git fixture or live token was run by Auditor |
| L4 first-party proposed workflow after installation on default branch | NOT ESTABLISHED; absent from master, no real new workflow master-push/dispatch/read-token/retained-artifact exercise yet |

**Separate mandatory Manager installation gate AFTER accepting this audit and independently authorizing Builder PR #162 integration**: reverify the exact final implementation SHA and current canonical Manager state, obtain successful post-merge FULL/master CI, then exercise the NEW first-party workflow (not the old removed manual one) with a controlled master push for one eligible checkpoint, a legitimate zero-eligible push, selected valid and invalid `workflow_dispatch`, actual read-only GitHub token/checkout and exact PR/head/ancestor provenance, preserved original packet/hash/trust anchor, safe negative-case PASS/FAIL/INFRA_ERROR artifacts, and multi-task aggregation where safely practical. Verify actual GitHub run/job/artifact IDs, failures and authorization limits; do not assert successful installation until they are observed. The workflow's push path filter requires Manager registry/task changes, so merely merging a workflow YAML without a corresponding trigger does NOT prove push behavior. A PASS here does not replace the existing separately verified TCW-034 manual readiness procedure.

**Final disposition: PASS — no independently established new blocking or nonblocking source-security findings on the exact frozen repaired target within the stated L1–L3 limits.** Historical TCW-048 FAIL remains immutable and applicable to its ORIGINAL failed SHA only; closure statements here refer to the NEW repaired SHA alone. Manager must separately consume this verdict and decide integration; do not merge either Auditor/Builder PR within this lane. Trade Builder PR #147 stays draft/unmerged; historical TCW-034 product audit target `035c5f5112b7393f9d4f17685792548afa67dd2e` and manual readiness remain unchanged; TCW-035 stays inactive.

Auditor evidence PR/exact-final-head CI: verify after exactly one authorized evidence commit and PR; publish actual IDs in the PR verification comment rather than inventing outcomes.
