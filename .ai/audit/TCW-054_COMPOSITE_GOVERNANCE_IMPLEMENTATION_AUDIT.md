# TCW-054 — Fresh Independent Composite Protected-Release GOVERNANCE IMPLEMENTATION Audit

Workflow V3.2 | Independent Auditor / QA | FRESH implementation/source-control lane | STANDARD_CHAT_HIGH / FAST_REFRESH | 2026-09-20 America/New_York

**CANONICAL INDEPENDENT VERDICT: FAIL — REMEDIATION REQUIRED on exact frozen TCW-053 executable source `324e5fe91d3749e882cd1aceb5ec2a4a73163123`.**

This is a *SOURCE-CODE/CONTROL-PLANE* verdict, NOT a finding that a live protected release happened or that current read-only `premerge`/ `postmerge` entrypoints issue a genuine release approval. They currently return RELEASE_HOLD. Two source-contract weaknesses must be fixed before Manager can accept the implementation as a future release-control component. Distinct external actor/ledger/original-helper/stage/installation prerequisites are still unavailable and remain separate blocking gates even after hypothetical code remediation.

## 1. Independent target, ancestry, canonical authority, allowed paths

- Independently fetched live `refs/heads/master` and assigned dedicated `refs/heads/auditor/tcw-054-composite-governance-implementation-audit` **BEFORE EVIDENCE WRITE**: both `25706f183e9d4523370748ca5ef8af459601bb60`; master...Auditor GitHub comparison IDENTICAL / ahead 0 / behind 0 / zero file diff. The actual historical Auditor branch creation `eed9dbb34a03e3493c12ccb0ebaf2e5106cba5c4` remains independently verified merge base of historical..Auditor, ahead 8/behind 0; it was never replaced by formal assignment-integration master. Historic zero-diff creation is asserted in the frozen packet and consistent with ancestry; current GitHub history is not a branch-reflog proof of the moment of creation.
- Canonical registry/task/packet jointly assign TCW-054 to this fresh Auditor branch, source task TCW-053/Builder PR #182, immutable audit target `324e5fe91d3749e882cd1aceb5ec2a4a73163123`, and EXACT two report/handoff evidence paths. Read `.ai/shared/WORKFLOW_V3_2.md`, `.ai/shared/ACTIVE_TASKS.json`, `.ai/roles/AUDITOR.md`, `.ai/manager/tasks/TCW-053.md`, `.ai/manager/tasks/TCW-054.md`, the frozen TCW-054 packet, original TCW-047, TCW-051 proposal/TCW-052 design verdict, original helper and Manager source-review freeze separately. Current active TCW-053 status **MANAGER_REVIEW_READY**, not AUDIT_READY/MERGE_READY.
- Builder #182 independently retrieved OPEN/DRAFT/UNMERGED at exact `324e5fe91d3749e882cd1aceb5ec2a4a73163123`; immutable Git tree independently fetched from native Git object `2c8147a0dfe945124e0acbd2cf338e31b9b2e9e2`. Original real Builder creation/authorized diff baseline `17cb363bf457d02cb0029430b110af002e43dc6a` must NOT become later Manager `eed9...` or current `2570...` baseline. Direct GitHub compare historic..Builder: ahead 7 / behind 0, 7 single-parent commits in exact linear chain `17cb363b→b6b1bdca→e77a6231→43bfbe92→c808d6b9→83d97dca→7636f487→324e5fe9`. Manager-only later activation commits were not inherited by the Builder branch.
- Exact SOURCE diff from ACTUAL baseline is only FOUR authorized paths: `.ai/shared/WORKFLOW_V3_2.md` (+15/−0, appended explicitly DORMANT TCW-053 appendix), `scripts/workflow-composite-release.js` (+611), `test/workflow-composite-release.test.js` (+505), `.ai/builder/TCW-053_HANDOFF.md` (+62). No changed registry, Manager source, original `scripts/audit-workflow.js`, original `scripts/workflow-audit-readiness.js`, Actions YAML, token/ruleset, trade/ESPN/product source, config or package files. Original source PR #162 OPEN/DRAFT/UNMERGED at `A=17e5f413f2afd3d743fd28d401f0df421825df2a`; trade PR #147 OPEN/DRAFT/UNMERGED at `035c5f5112b7393f9d4f17685792548afa67dd2e`. Historical original TCW-047 true Builder creation `7ca2953009d37a014e041cc24f4934bfe61b5cad` differs from explicitly approved effective scope baseline `e0fe6309dc0aaa184bbeef35861f7d49256385b7`; past TCW-050 source PASS applies A ONLY, never TCW-053 Builder head or future stage S/G.
- Canonical assignment-integration master `25706f183e9d4523370748ca5ef8af459601bb60` had genuine master push workflow #763/run `35513997243` completed SUCCESS. Exact Builder PR #182 final FULL workflow #761/run `35512711959` / required test `106083335689` completed SUCCESS at frozen SHA, direct job logs say `1..492; # pass 492; # fail 0`, effective mode FULL, V3.2 workflow-audit and standard model/smoke/security/classifier stages succeeded, deploy/production skipped on PR. Retained artifact `tcw-ci-evidence-35512711959-1` ID `10605862390`, not expired. Earlier synthetic-fixture run `35512667724` failed two cases and remains historical FAIL corrected at final head. No independently executed original task-specific mechanical helper packet has been observed. Green FULL CI does not defeat the counterexamples below.

## 2. Exact code reviewed and confirmed safe boundaries

Read all 611 source lines and all 505 test lines at immutable Builder commit (file Git blobs `scripts/workflow-composite-release.js=b1c62a5d3aca8d7808ca23547453a4e337cd0754`; test code at exact frozen ref); compared documented appended appendix with inherited V3.2. `validateLocalContract`, `validateLedgerTransition`, `validatePremergeSnapshot`, `validatePostmergeSnapshot` explicitly return LOCAL_* classifications for caller-provided fixtures, not release authority. Source `verifyPremergeReadOnly` calls a local contract check then GET-only stage observation; both its normal and error paths return FAIL or RELEASE_HOLD, never RELEASE_READY/GO. `verifyPostmergeReadOnly` similarly holds irrespective of synthetic/partial G evidence. CLI `validate` exits 0 for LOCAL_CONTRACT_PASS, `premerge`/`postmerge` exit 3 for RELEASE_HOLD; no merge/dispatch/ref update/credential/publish/ledger-write or stage creation API was found in the four-file source diff. No new Actions workflow is on canonical default branch; real installed L4 is NOT VERIFIED. This active hard HOLD limits the present exploitability of the findings; it does **not** turn an incorrect declared contract predicate into a future verified release guard.

## 3. TCW-054-F01 — MEDIUM / BLOCKING — original source A audit and packet are not authenticated against canonical frozen custody

**Contract violated:** TCW-053 mandatory F01 and frozen TCW-054 packet require a distinct *immutable audited* original source A and original helper/TCW-050 evidence; no old pass may be re-targeted to a new Builder SHA. A self-consistent new JSON claim is not evidence of a valid freeze.

**Actual code:** `validateLocalContract` at `scripts/workflow-composite-release.js` source lines ~157–184 requires original `source.pr=162`, branch and historical creation, but accepts **any** 40-hex `source.sha`/tree, **any** 64-hex `source.packet.sha256`, any distinct 40-hex effective scope baseline and any 40-hex `source.audit.evidenceCommit` paired with a claimed PASS on the self-chosen `source.sha`. `releaseTuple` binds these self-chosen fields by hashing, which authenticates internal consistency, not approved provenance. `observeStageReadOnly` (~481–535) fetches source PR/ref/commit/tree and four source blobs, but does not retrieve the canonical Manager source checkpoint `A=17e5f413...`, original packet SHA-256 `f6d59762e3696f91696408e5312013481fb1dc5e9dd24d1ee469b1f590f96894` or accepted TCW-050 immutable source-audit report/commit and original helper run. `validatePremergeSnapshot` (~299–406) checks a same-SHA current source PR/ref and stage custody but does not independently join those trusted original source audit/packet records.

**Reproducible source-level counterexample (NOT an executed live release):** Starting from the included `fixture()` in `test/workflow-composite-release.test.js` lines ~35–112, choose a distinct syntactically valid 40-hex replacement A; set `source.sha/source.tree`, `source.packet.head`, `source.audit.targetSha`, packet digest/evidence SHA to internally consistent but unaudited values, and replace the four source+stage blob tuples coherently. Recompute `ledger.tupleDigest=tupleDigest(a)` and transition-event tuple digests. The existing local conditions test equality, schema/shape and digest; there is no frozen A/accepted packet/actual TCW-050 evidence check that rejects this replacement. An independently observed source PR which had actually moved to the new A would also satisfy the source-ref/tree portion of `validatePremergeSnapshot`. **Even if source PR changes, the current hard RELEASE_HOLD remains in force**, so this proves a missing future custody predicate rather than an accomplished merge bypass.

**Impact:** A future release implementation could incorrectly treat an unreviewed changed source/head plus a forged old-audit claim as equivalent to accepted TCW-050 if it builds on these passing local/live-blob predicates. Current exact source PR #162 is unchanged, and no such release was performed.

**Required bounded remediation:** Bind A to a separately authenticated/frozen canonical TCW-047 checkpoint/PR/branch and true creation/effective baseline. Independently retrieve and validate the original packet bytes/digest (not merely a self-supplied digest), actual TCW-050 report blob/commit/target/accepted verdict and original helper run/job/packet provenance. If these cannot be authenticated inside currently approved read-only/four-file scope, make the source-custody proof an explicit **unverified HOLD** prerequisite, not an apparent locally complete source audit. Add negative tests for an advanced source head, fabricated packet hash, changed effective baseline and unrelated/fake audit-commit claim even when the tuple digest is recomputed. Preserve historic A-only PASS without changing TCW-047 registry or Builder branch.

## 4. TCW-054-F02 — MEDIUM / BLOCKING — branch owner and live no-bypass PR-protection checks are weaker than the declared release contract

**Contract violated:** TCW-053 F01/F02 require a distinct *Manager-owned* stage and actual strict PR-protected/no-bypass GitHub rules, not just a string claiming `bypass:false` and one surviving required check.

**Actual code:** `validateLocalContract` ~195–207 uses generic `REF` to accept any `refs/heads/*` stage ref, rejects source branch and literal master but never checks a `refs/heads/manager/` prefix or separately scoped authorized Manager stage task/branch. `validatePremergeSnapshot` ~314–326 verifies stage PR/ref/head/base agreement, not Manager branch ownership or an authenticated Manager assignment. Its `l.ruleset` test ~334–341 checks active enforcement and existence of a strict required `test` entry app 15368; it does **not** verify `pull_request` remains required, `bypass_actors` is empty, `current_user_can_bypass=never`, or that other applicable effective rules have not been weakened. A separate `a.ruleset.bypass === false` is a self-reported attestation property, **not** a check of live `l.ruleset.bypass_actors`. Exact digest equality only proves the supplied snapshot matches the *current* ruleset bytes, not that those bytes satisfy release policy.

**Reproducible source-level counterexamples (NOT exercised against GitHub protection):** (1) On test `fixture()`, change `stage.branch` to `refs/heads/auditor/composite-test-stage`, recompute ledger/event tuple digests and set the mocked live PR head ref to `auditor/composite-test-stage`. Both the general REF pattern and the current branch equality predicate allow the non-Manager staging identity. (2) From `liveFixture(a)` keep required `test` strict, but delete its `pull_request` rule and add a bypass actor to `live.ruleset.bypass_actors`; recompute attested ruleset digest and immutable tuple/ledger digests. The live ruleset predicate remains true because it inspects neither property. The real current ruleset #22309639 *does* include PR-required/no-bypass, and the real release remains HOLD; this is a negative-validation gap, not a claim current protection was weakened.

**Required bounded remediation:** Validate stage full ref against an independently authorized Manager-owned stage task/branch/PR (not prefix alone, if ownership could change); check actual effective protected branch rule(s) include PR-required, strict up-to-date required `test` app 15368, no bypass actors/current-user bypass, and allowed/selected explicit `merge` method, with fail-closed unavailable-permission cases. Authenticate source/status of retrieved rules rather than relying solely on a copied `bypass:false`; add fixture tests for Auditor/Builder-controlled S, missing PR requirement, newly added bypass actor, partial/cross-ruleset protection and changed ruleset digest.

## 5. Separate unresolved prerequisites — NOT substituted for code findings

- **Original TCW-053 helper:** canonical TCW-053 is only MANAGER_REVIEW_READY. The original `npm run workflow:audit-readiness -- --task TCW-053` has NOT run on the exact assigned Builder checkout with an isolated READ-ONLY overlay of the real canonical Manager registry/task/spec. No genuine `TCW_AUDIT_READINESS_V1` packet, digest or `readyForManagerFreeze=true` is established. The FULL CI generic workflow audit and GitHub branch/path prechecks are not the task-specific original helper. Auditor did not assert or manufacture this PASS. STOP promotion to AUDIT_READY/MERGE_READY, protected integration, task closeout or release absent a real separately verified original-helper packet or a separately authorized alternate bounded gate.
- **External F01:** independently protected durable nonce/attempt/consumed/aborted ledger, GitHub actor/owner/Manager/Auditor publishing permissions or cryptographically attributable separated roles, off-master evidence ref immutability and retention, real later exact-S owner consent and rollback operator have NOT been built or verified. The local `protected:true`/actor IDs/mock `authenticated:true` are not external permission proof. Additional files, credentials, identities, services or storage require a NEW separate owner/Manager scope and independent audit. The current module's unconditional RELEASE_HOLD is the correct immediate behavior; do not replace it with LOCAL_* success.
- **External F02:** no future stage-source PR or independently audited actual S, no exact-S FULL required test/real GitHub synthetic preview, no real protected two-ordered-parent G/tree/master postmerge, no real FULL master/deploy/production/forward rollback or consumed protected ledger; old TCW-050 source PASS cannot transfer. GitHub `expected_head_sha` is not an atomic base-M CAS; any master/S/ruleset/check drift requires STOP/new freeze.
- **Live safety/limits:** retrieved live ruleset #22309639 remains active PR-required, strict required `test` integration 15368 and no bypass actors. Genuine first-party new readiness workflow is absent from canonical master, so L4 is not established. This Auditor did not create/merge source PRs, stage or install workflow, change permissions, run dispatch/deployment, modify product/ESPN/trade or perform protected ledger writes.

## 6. Verification matrix and exact disposition

| Dimension | Result | Distinction |
| --- | --- | --- |
| Formal branch equality/true historical creation | PASS | `25706f183e9d4523370748ca5ef8af459601bb60` master == Auditor before writing; real creation `eed9...` preserved as ancestor |
| Source target/ancestry/tree/four-file scope | PASS | Exact Builder `324e5fe91d3749e882cd1aceb5ec2a4a73163123`, tree `2c8147a0dfe945124e0acbd2cf338e31b9b2e9e2`, true base `17cb...` |
| Exact Builder final-head FULL CI | PASS (automated tests only) | #761 run `35512711959`, test `106083335689`, 492/492 PASS, retained artifact `10605862390` |
| Independent source/control-plane correctness | **FAIL — REMEDIATION REQUIRED** | F01 source trust boundary and F02 stage/actual protection predicates |
| Original task-specific mechanical readiness | **NOT EXECUTED / PENDING** | No authentic original `TCW_AUDIT_READINESS_V1` packet/ready flag |
| External protected actor/ledger/off-master authority | **NOT ESTABLISHED / RELEASE HOLD** | No role rights/protected durable receipts; not authorized in current scope |
| Actual S, G, master production/rollback, installed Actions L4 | **NOT PERFORMED / NOT AUTHORIZED** | No live staging or installation |
| Auditor evidence PR exact FINAL head CI | **PENDING WHEN REPORT AUTHORED** | Verify after both evidence files committed; publish immutable run/job in PR comment + Manager return; do not edit source or manufacture tests |

**Disposition to Manager:** Treat F01/F02 as blocking material implementation-source findings; independently accept/reject each and, if accepted, scope ONLY bounded repair of `scripts/workflow-composite-release.js` plus adversarial `test/workflow-composite-release.test.js` within TCW-053, then freeze a NEW exact Builder SHA and commission a NEW fresh independent code audit. Do not merge Builder #182 on this FAIL, do not self-merge this evidence PR, and do not silently broaden to external ledger/actor infrastructure. Separately establish original-helper mechanical evidence before further source integration and retain the external release HOLD until independently authorized/proved. Keep TCW-047 OPEN/uninstalled, Builder PR #162 and trade #147 unchanged DRAFT/UNMERGED, TCW-034 manual readiness authoritative and TCW-035 inactive.

This report and `.ai/auditor/TCW-054_HANDOFF.md` are the ONLY authorized Auditor writes.
