# TCW-052 — Fresh Independent Protected-Release Protocol Governance DESIGN Audit

Workflow V3.2 | Independent Auditor / QA | Fresh audit lane | STANDARD_CHAT_HIGH / FAST_REFRESH | 2026-09-20 America/New_York

**CANONICAL VERDICT: PASS WITH NON-BLOCKING FINDINGS — NON-OPERATIVE DOCUMENTARY DESIGN ONLY.**

The separate A (source) / S (stage) / G (integration) architecture is logically coherent **only as a conditional candidate for a later separately authorized, implemented, independently audited governance exception**. Nothing in this verdict means current V3.2/TCW_TASK_V2 already allows composite release binding, that an actual S/G has been created or tested, that a staged security audit has passed, that GitHub will merge it, that installation has been approved, or that first-party readiness Actions L4 works. All future gates below are fail-closed. If the scoped implementation cannot enforce the binding, source custody, repository permissions, and required-check sequence, STOP and return to Manager for redesign and a new exact-target governance audit.

## 1. Independent identity, authorization, and scope verification

- Read live GitHub `refs/heads/master` and `refs/heads/auditor/tcw-052-protected-release-governance-audit` twice before evidence write: both **exactly** `a1108939b691db3c479268631e9047b0c3dd44bf`. Direct master..Auditor comparison is IDENTICAL: ahead 0, behind 0, no files. Historical Auditor creation `3c93bac1e3d9fba0d5600e30f440757f2e27401a` is the independently verified merge base of historical..Auditor, ahead 8/behind 0; formal integration was non-force forward from that ancestor. The original creation baseline stays `3c93...`, NOT `a110...`. Initial zero-diff branch creation is documented in frozen Manager packet; the current GitHub ref/ancestry proves the historical ancestor, not a historical reflog snapshot of the moment the branch was created.
- Frozen target is **Manager design commit** `c7b0923f5dfcf0a1e7ce5ac3b7d301b4de96cd75`, `manager/tcw-051-protected-release-protocol-design`, PR **#177 MERGED**, source creation base `5b1259e350c5b35d97d6542784e1f97869f0ae83`, one net added path ONLY `.ai/manager/evidence/TCW-051_PROTECTED_RELEASE_PROTOCOL_PROPOSAL.md`. Git blob at frozen target AND current master is `a38cc9aeccd7b7650cd7704b8788b8a96178db9b`. GitHub #177 reported merge integration `3c93bac1e3d9fba0d5600e30f440757f2e27401a`. Exact proposal PR run #748 / `35490042285` / test job `106023311829` SUCCESS (DOCS_ONLY as recorded by packet); post-integration master #749 / `35490067234` / test job `106023373172` SUCCESS, deploy and verify-production jobs SKIPPED. These are evidence of documentary integration, not execution of release design.
- Canonical V3.2, V3.1, base WORKFLOW, Manager TCW-047/051/052, frozen TCW-052 packet, Auditor charter, current registry, original readiness/static validators, current deploy-pages workflow and prior TCW-050 report read independently. Registry TCW-052 ASSIGNED, target PR 177 / exact SHA `c7b...`, historical creation `3c93...`, exactly two allowed new evidence files, Manager-only merge authority. TCW-051 remains non-operative and audit-required; TCW-047 remains OPEN/WAITING_EXTERNAL_EVIDENCE and uninstalled.
- GitHub Protect Master ruleset **#22309639**, fetched directly at `https://api.github.com/repos/Ryan42062001/the-chip-winner/rulesets/22309639`: active on default branch, PR required, strict up-to-date status checks true, required `test` from integration `15368`, no bypass actors, `current_user_can_bypass=never`; allowed merge methods merge/squash/rebase. Separate branch-protection endpoint returned 403 to this connector; the accessible live ruleset is the positive evidence for these enumerated controls. This snapshot must be fetched again for an actual release, including any other applicable rules.
- Source Builder PR **#162 OPEN/DRAFT/UNMERGED** at exact `A=17e5f413f2afd3d743fd28d401f0df421825df2a`; source TCW-050 PASS is bounded pre-install L1–L3 at A only. Original true Builder creation/first assignment `7ca2953009d37a014e041cc24f4934bfe61b5cad`, later explicitly approved effective verification baseline `e0fe6309dc0aaa184bbeef35861f7d49256385b7` (not creation), and sync merge `20f372a0387bf51eff33ef6957aef1f45a144ab5` remain distinct. Historical TCW-048 FAIL at `acb63b0c85b98b34fac9af99f00f38553de5670c`, TCW-049 PASS at `7c5bd1add860d1e8ed7bc03717451c7d88a21c50`, TCW-050 PASS at A never transfer to S/G.
- Trade Builder PR **#147 OPEN/DRAFT/UNMERGED** at historical `035c5f5112b7393f9d4f17685792548afa67dd2e`; no trade/ESPN/field-validation or site product source changes authorized. `.github/workflows/task-audit-readiness.yml` returned NOT_FOUND on live default master: new workflow is NOT INSTALLED. Owner's authorization is DESIGN + FRESH INDEPENDENT GOVERNANCE AUDIT ONLY. The present Auditor has not staged, installed, dispatched, deployed, or approved release.

## 2. A / S / G identity and Git-tree analysis

**A** is the real assigned Builder PR #162 source and original task-specific readiness/checkpoint target. The original `worker_checkpoint_sha=A`, `audit_target_sha=A`, assigned `builder/...` branch, original helper packet SHA-256 `f6d59762e3696f91696408e5312013481fb1dc5e9dd24d1ee469b1f590f96894` and original mechanical run `35488171554`/job `106018254992` describe A, not S. An `A` source PASS alone cannot approve a composite stage.

**S** would be a *new*, Manager-owned staging PR's immutable final head, from a freshly frozen master M. S must contain M and be current with M, have its own Git tree and FULL required check, and be independently audited over the **whole** staging diff. Its source-file blobs must equal the four authorized original A file blob IDs, including the historical Builder handoff as a historical immutable source record. Manager's separate release attestation must not impersonate an updated Builder handoff. Any other staged path must be separately pre-authorized, included in the full stage audit, and handled by a separately audited governance implementation. A source-blob mismatch or source/test/permission change is NEW source work, not a byte-identical transplant.

**G** would be GitHub's resulting protected integration commit. Use only the specifically approved `merge` method to preserve auditable first parent M and second parent S; neither `squash` nor `rebase` preserves this proposed two-parent identity contract. Fetch actual postmerge Git commit G, assert parent1 == the exact approved M and parent2 == S, `G.tree == S.tree`, and live master ref == G. Independently compare full paths/blobs and the actual merge diff to M, plus subsequent master test/deploy/production. Equality of Git tree IDs proves identical committed files, NOT correct parentage, permitted reviewers, correct GitHub test integration, effective protection, deployment success, or runtime operation. GitHub's synthetic merge preview may be a different SHA from PR head S and is not G; verify both PR head and whatever preview/check context GitHub actually requires. This method and its actual ruleset/test compatibility are unproven until a specifically authorized real stage exists.

**Self-reference:** Requiring S's SHA in the file tree which determines S creates circular binding; no such fixed-point is established or required. Freeze S in a DIFFERENT immutable evidence commit whose content contains S; keep that evidence commit off master until S merges. The same off-master rule applies to the independent Auditor report while strict up-to-date status checks are required.

## 3. Minimum external premerge attestation and trust model — required future implementation contract

A future *separately governed* machine-readable, versioned `TCW_COMPOSITE_RELEASE_ATTESTATION_V1` must carry at minimum:

- `repo_id` plus exact `repo_full_name`; release-task ID and independently approved exception/policy commit+blob; owner-approved staging-plan authority ID/immutable evidence commit; Manager evidence-branch full ref AND exact content commit SHA/blob/tree; Manager role/actor and authenticated publication method, creation time, expiry or explicit fresh-attempt nonce; expected current master `M` SHA/tree and ruleset ID plus a digest of all applicable required conditions.
- Source `A` full SHA, source PR #162/repo/base/assigned branch, actual historic Builder creation and distinct effective baseline, original four path/mode/blob tuples, immutable original task-specific packet SHA-256, original FULL run/job, accepted TCW-050 source audit exact evidence SHA and scope.
- Stage PR number, repository, `head_ref`, exact `S` SHA/tree, base ref master and exact base `M` SHA, entire stage file list with status/mode/blob and permitted Manager metadata, separately scoped whole-stage provenance validator version/hash and PASS evidence; exact FULL required `test` check-run ID/run/job/check-suite/context/app ID, `head_sha`, conclusion, verified PR identity and base M; if GitHub tests a synthetic merge commit instead, explicitly record and validate its exact parents/trees too.
- Separate Auditor branch/PR, true creation baseline, independently committed report/handoff SHAs/blobs, target S/M/tree and verdict, exact auditor final-head CI; distinct Manager acceptance and **later** exact-S owner installation consent referencing exact stage PR/S and current rollback operator; allowed merge method `merge`, expected parent1 M/parent2 S and expected postmerge tree, abort/reconciliation protocol.

The attestation commitment must be **an immutable Git object ID verified by independently fetching its bytes and parentage**, not only a branch name, comment, claimed SHA, URL, unverified Git author name, or unsigned user-provided text. Git object SHA authenticates *bytes*, not a person's authority: establish authorized publishing identity/permissions, approved signer policy if available, reviewer separation and explicit owner consent via independently retrieved GitHub evidence. Current same-account chat roles are procedural independence, not separate cryptographic principals; do NOT assert cryptographic role separation without verified separate credentials/signatures and their trust policy. Restrict write access to the intended evidence and audit branches where practicable, prevent or detect non-fast-forward ref replacement and deletion, retain exact commit IDs and archive evidence after merge; an old SHA being fetchable does not establish the ref was not moved. Evidence-branch HEAD must be exact expected commit at use; the audit commit must have authentic recorded provenance, independently verified distinct role and unchanged report content.

Replay defense: require one new per-attempt nonce, tuple `(repo_id, release_task, M, stage_pr, S, S.tree, ruleset_digest, attestation_commit, independent_audit_commit)`, an unconsumed/active release state bound to this tuple, and short validity as defined by the approved machine policy. Reject any reused nonce or consumed/aborted attempt, foreign repo/ref/PR, stage branch moved, unexpected evidence ref commit or amended report, S/tree/paths/CI mismatch, old/stale base M or ruleset change, missing authorization or owner consent for exact S, and ambiguity in audit/attestation lineage. Store the *consumed* tuple and actual G in protected Manager postmerge closeout so a repeated approval cannot be replayed as new authority. A branch PR comment can link immutable evidence, but is not its trust anchor.

This schema/replay policy is a **review recommendation and mandatory implementation proof obligation, NOT an active exception**. `scripts/audit-workflow.js` currently validates ordinary registry shape/owner/branch, audit target metadata, unique slots/PR and lifecycle but has no composite-release attestation schema, signer/replay/state or A→S binding validation. `scripts/workflow-audit-readiness.js` explicitly checks assigned branch, HEAD against the original Builder checkpoint, `assignment_master_sha..HEAD` scope and task eligibility; it cannot legitimately run unchanged on a different Manager-owned staging branch and claim the A helper PASS. Do not change TCW-047 `worker_checkpoint_sha` or `assignment_master_sha` to S, rewrite historical baseline or assign PR #162's identity to the staged PR.

The future material governance implementation must define a separate **Manager composite-release task/state machine** and its validator/tests, preserving ordinary TCW_TASK_V2 invariants for all other tasks. It must pass the normal static workflow audit on master and staged HEAD; validate rejected stale/replayed/missing/wrong-repo/wrong-branch/wrong-PR/changed-blob and incomplete-closeout cases. It must be independently security/control-plane audited and guarded-integrated, with real master CI, BEFORE source staging is authorized. A mere prose exception or untested release-time waiver is insufficient.

## 4. Adversarial sequencing, races and authenticated checks

1. **Today:** only documentary proposal #177 is integrated; TCW-052 is design-only. No released S, G, governance exception, owner staging authority, or owner installation consent exists.
2. **Separate future decision:** Manager decides whether to commission a bounded material governance implementation; distinct fresh auditor audits its exact committed machine/validator behavior; normal protected merge+master CI integrates it. If this fails, STOP. A separate owner authorization must precede creation of any stage-source PR.
3. Freeze live protected M, current active rules/ruleset digest, original A/PR #162 and four exact source blobs, task/branch/PR custody and rollback readiness. Create DRAFT stage PR based on exact M and only approved source blobs plus any separately authorized, implemented governance metadata. Stage full diff and tree, including mode changes, must be independently enumerated. Assert M is an ancestor of S and stage PR targets same repository/master. Do not change A or run original Builder helper on S as if it were assigned Builder PR #162.
4. Full `Deploy website / test` MUST run for actual FINAL stage S, not only source A nor DOCS_ONLY, using existing GitHub Actions expected app/integration 15368 and stage PR. Read the actual GitHub check run and required GitHub merge preview status; verify `head_sha`, check suite/app, run/job/conclusion, PR number/head/base and unchanged ruleset. The current workflow has `jobs.test` always present, but a future first-party workflow addition is non-Markdown and therefore requires FULL mode. A green old Builder run, docs-only audit run, manually set commit status, or test of a different merge preview is NOT the required final-stage proof.
5. Freeze S/M/tree and stage checks. Independently audit S's FULL final composite diff against exact M and A; record off-master distinct Auditor commit/PR and off-master Manager attestation commit. These must **not** advance master or alter S. Audit report commits to master BEFORE staged merge -> master M' -> strict up-to-date S invalidated (the original loop); do not do it. Any stage S head change, master M movement, ruleset/test/context change, source A ref/bytes change, audit report change or security finding voids the attempt. Do not resync S and reuse old S audit.
6. After exact audit PASS and authenticated evidence, Manager must re-fetch all refs, trees, CI and permissions immediately before release and ask for a **separate explicit owner installation authorization naming stage PR and exact S**, the new first-party workflow's read-only token and likely master-push website deployment/rollback. This consent is NOT the design/audit or future staging-plan consent. No rollback operator/plan -> STOP.
7. On separately authorized go, Manager may mark the stage PR ready, immediately re-check the tuple, then invoke ordinary guarded GitHub PR merge using `merge` and `expected_head_sha=S`. GitHub connector supports expected-head CAS for S but does **not** expose an atomic expected-*base* CAS: live master/ruleset/race rechecks and GitHub's strict protection are required. A concurrent master update between read and merge may cause GitHub to reject; do not bypass, auto-merge, loosen strict checks or retarget silently. If GitHub merges with unexpected M/G/tree, treat as installation incident, STOP, preserve evidence and route owner-supervised forward revert. A failed 405 leaves draft/unmerged source and requires a NEW review/freeze, not an old-green retry.
8. Independently fetch G/master/parent1/parent2/tree, actual FULL master test and website deploy/production smoke after any deployable new workflow path. Then merge separately retained Auditor evidence through ordinary protected PR, reconcile A/S/G, actual source PR #162 disposition, Manager registry and task states without pretending #162 merged. Treat installation as **INSTALLED**, not **OPERATIONAL**; the later control-plane docs-only audit-evidence merge cannot stand in for the G master validation.
9. Distinct supervised real installed first-party L4: safe new disposable non-product eligible task, genuine canonical push trigger and valid/invalid dispatch, token permissions, PR/ref/ancestor/original packet integrity, retained per-task/global PASS/FAIL/INFRA_ERROR/NO_ELIGIBLE_TASK and adversarial negative cases. NO_ELIGIBLE_TASK is not positive ready-task PASS. Current connector exposes no direct workflow-dispatch action; a separate authorized UI/credential/operator plan is necessary, otherwise mark L4 UNVERIFIED. Never use trade TCW-034 / PR #147 or fabricate authenticated test proof.

**Counterexamples:** (a) merge Auditor evidence to master after stage FULL test and before stage merge -> stage now behind -> strict required check blocks; (b) copy original four A blobs but append a new unaudited Manager validator to S -> historical source PASS does not cover altered tree; (c) green `test` on source A or earlier S1 while final S2 changes -> not exact-head PASS; (d) mutable external ref/comment points at different attestation/report while a stored SHA is unchanged -> reject current ref/custody mismatch; (e) G has S.tree but parent1 != M or parent2 != S -> tree alone insufficient; (f) an unaudited release exception bypasses original A/assigned Builder helper -> unauthorized role/target redefinition; (g) master FULL passes but actual installed default-branch first-party action never runs -> L4 remains unverified.

## 5. Findings and explicit future gates

- **TCW-052-F01 — LOW / NON-BLOCKING FOR THIS DOCUMENTARY DESIGN:** The frozen proposal outlines off-master attestation and states future schema/replay/owner/signature verification requirements but does not itself provide executable versioned fields, replay ledger, immutable author/role trust policy, or negative validator tests. For future GOVERNANCE ACTIVATION, these are **BLOCKING UNIMPLEMENTED PREREQUISITES**; implement the bounded tuple/state contract above, reject replay/forged/moved evidence, preserve ordinary task freeze and independently re-audit the actual implementation. A future Manager cannot infer that this report installed those controls.
- **TCW-052-F02 — LOW / NON-BLOCKING FOR THIS DOCUMENTARY DESIGN:** The proposal allows discussion of different protected GitHub merge semantics and checks G.tree == S.tree but must explicitly constrain the executed release to an actual verified two-parent `merge` path, parent1 M/parent2 S, while checking the real GitHub stage HEAD and test-merge required check. Ruleset presently allows squash and rebase too; absent bounded activation of an explicit merge-only procedure, acceptance of an arbitrary allowed method could break the proposed ancestry test. Future stage go remains BLOCKED pending an independently audited exact algorithm and genuine GitHub check/merge behavior verification.

**Validation matrix:** documentary source scope/blob and live refs PASS; active ruleset PASS via ruleset API (separate branch-protection endpoint 403); independent logical adversarial governance review PASS WITH NON-BLOCKING FINDINGS; source proposal PR #748 test SUCCESS and master #749 test SUCCESS (documentation provenance ONLY); new exception/static-validator implementation NOT PERFORMED; actual S/full same-stage test/independent S audit NOT PERFORMED; G/tree/production/rollback NOT PERFORMED; installed new Actions L4 NOT PERFORMED. Auditor evidence PR final-head CI: PENDING AT REPORT AUTHORING, to be verified and reported in the separate handoff/Manager PR comment without altering frozen findings.

## 6. Independent disposition / Manager handoff

**PASS WITH NON-BLOCKING FINDINGS applies ONLY to the coherent conditional DESIGN at c7b0923f5dfcf0a1e7ce5ac3b7d301b4de96cd75.** Manager may independently review this report and decide whether to propose a narrowly scoped separate *governance implementation* with its own frozen exact SHA and fresh independent control-plane audit. No stage/source PR creation, release exception activation, protected merge, installation or L4 approval is implied; the owner has authorized none of those by commissioning TCW-051/052. Keep TCW-047 OPEN, Builder #162 and trade #147 DRAFT/UNMERGED, TCW-034 manual readiness authoritative, TCW-035 inactive, and preserve source audit SHA scopes. Auditor changes ONLY this report and `.ai/auditor/TCW-052_HANDOFF.md`; one distinct evidence-only PR must remain UNMERGED for Manager.
