# TCW-051 — Protected-master release protocol: DRAFT GOVERNANCE DESIGN FOR INDEPENDENT REVIEW

Workflow V3.2 · Manager / Architect · 2026-09-20 America/New_York
**Proposal status: NOT ACTIVE / NOT AUDITED / NO INSTALLATION OR STAGING AUTHORITY.**
Owner authorization is strictly to DESIGN and commission independent governance audit. It does not authorize creation/merge of a composite staging-source PR, PR #162 merge, Builder resynchronization, deployment, GitHub ruleset edits, write-credential elevation, data-source changes, or any new release SHA. A future material workflow/registry/audit-authority amendment requires an independently accepted governance verdict and separate Manager integration before this design may be used.

## 1. Observed hard problem and immutable scope

Canonical design baseline `5b1259e350c5b35d97d6542784e1f97869f0ae83`. GitHub's active Protect Master ruleset #22309639 requires PR, strict **up-to-date** GitHub Actions `test` (integration ID 15368), no bypass. Existing Builder PR #162 is OPEN/DRAFT/UNMERGED and BEHIND canonical master. It has an independent TCW-050 bounded L1–L3 PASS for exact Builder source `17e5f413f2afd3d743fd28d401f0df421825df2a`, genuine FULL #738/run `35487865670`/test `106017434730` SUCCESS, separate ORIGINAL task-specific mechanical PASS run `35488171554`/job `106018254992`, packet SHA256 `f6d59762e3696f91696408e5312013481fb1dc5e9dd24d1ee469b1f590f96894`. Accepted Auditor report `.ai/audit/TCW-050_SYNCED_READINESS_SECURITY_REAUDIT.md`, PR #175 merged evidence only. These facts DO NOT constitute a PASS for a future changed commit or installed first-party Actions L4.

Original ACTUAL Builder branch creation/first assignment SHA `7ca2953009d37a014e041cc24f4934bfe61b5cad` is immutable. Later owner-approved effective scope verification baseline `e0fe6309dc0aaa184bbeef35861f7d49256385b7` is NOT creation; sync merge `20f372a0387bf51eff33ef6957aef1f45a144ab5` has old repaired parent `7c5bd1add860d1e8ed7bc03717451c7d88a21c50` and canonical effective parent. Historical TCW-048 FAIL at `acb63b0c85b98b34fac9af99f00f38553de5670c`, TCW-049 PASS only at older repaired SHA, TCW-050 PASS only at new synchronized SHA each remain separate.

The currently authorized TCW-047 task + registry bind `pr:162`, actual assigned Builder branch, immutable `worker_checkpoint_sha`, `audit_target_sha`, original helper and current effective baseline to **the Builder source SHA**, not to an as-yet-nonexistent composite Manager staging PR. The release attempt at an older audited head failed GitHub HTTP 405 because it was behind. Syncing Builder to M creates B; recording new B and audit evidence on canonical M' makes B behind M' again. A proposed composite PR head S is distinct from source Builder A; its own 40-character SHA cannot be embedded as a self-referential value in the SAME pre-merge Git tree that determines S. A GitHub merge commit G is a third identity even when G.tree == S.tree. Treating A, S and G as identical is forbidden.

## 2. Proposed narrow protocol — THREE separate SHA identities and an EXTERNAL premerge audit binding

**A = vetted source Builder commit** (current `17e5f413...`) remains immutable on original Builder PR #162; source task retains actual historical creation and distinct effective diff baseline, accepted TCW-050 verdict and original helper packet. A source audit proves neither future composite staging HEAD S nor future merged master commit G.

**S = proposed composite stage HEAD** is a separate NEW Manager-controlled PR branched from then-current master M. It copies only the four *exactly verified original Builder file blobs* from A (including the historically dated Builder handoff as immutable source evidence); no unrelated product, trade, ESPN, package, helper, validator or deployment edits. A staged candidate MUST NOT modify canonical registry to falsely assert that its distinct HEAD S equals source Builder A or impersonate Builder PR #162; any proposed metadata exception/binding is independently reviewed as a specifically approved governance change BEFORE staging. If source handoff's old SHA is historical, the Manager release attestation must explicitly identify it as historical, not falsify handoff contents.

**G = protected resulting integration commit** from GitHub's approved guarded PR merge of S. G will generally differ from S. The only acceptable equivalence for the audited installation tree is independently comparing exact `git tree` objects G.tree == S.tree and ensuring G's parentage includes exact frozen master M and S under the approved merge method, with NO concurrent master movement. If resulting merged tree is not exact independently reviewed stage tree, fail the release and route remediation; do not assert post-merge source acceptance.

**External exact-stage binding proposal:** freeze S by recording `stage_pr_number`, exact S commit SHA, S Git tree SHA, exact base master SHA M, old A/Builder PR #162 and four original blob IDs, full stage changed-path list, final required `test` run/job/check-run context, active ruleset, historic provenance and original packet hash in a dedicated append-only release attestation **committed on a separate Manager evidence branch** (or another independently verified immutable Git commit). It is *not* embedded into S's own tree and *not* merged to protected master before S merges. Exact S/audit evidence is authenticated by Git commit SHA, PR head/ref/repository, auditor commit SHA and independent GitHub API retrieval; a mutable PR comment alone is NOT a tamper-proof attestation. An approved exception to current `TCW_TASK_V2` machine registry is necessary before using this design; it would allow an explicitly modeled pending composite-release attestation to be held outside canonical master without overwriting `worker_checkpoint_sha=A` in the pre-install registry. This proposal DOES NOT itself create/authorize that exception or change the validator.

## 3. Required sequence; why it should avoid the known stale-branch loop

G0. **Separate governance gate first.** Manager authors a narrowly bounded explicit policy/change record covering the external frozen S binding, source A→stage S custody, evidence branch owner, release authority, branch+CI verification, exact-tree integration, postmerge reconciliation and all failure/abort states. Independently audit the exact proposed policy/validator/machine-state behavior in a FRESH workflow/control-plane lane. Material changes remain INACTIVE until accepted and canonically integrated with their own successful master CI. If Auditor fails, STOP. Do NOT create stage source or use a release exception beforehand.

G1. **Owner staging-plan authorization is separate from owner installation authorization.** After accepted governance, separately authorize creation of a DRAFT, unmerged Manager-owned proposed staging PR if required; owner approval of today's design/audit does not authorize that future source staging. Freeze live master M, required ruleset, rollback operator availability, current source A/PR #162/DRAFT state and four source blob identities. Do not run a trial install, enable auto-merge, alter branch protection, or touch trade Builder PR #147.

G2. Create proposed stage S from exact current M using only authorized source files and explicitly approved governance metadata under the independently accepted contract; record S, M, tree and complete diff. Stage PR remains DRAFT/UNMERGED. The existing `scripts/workflow-audit-readiness.js` original Builder preflight on A and prior TCW-050 PASS cannot be called an original helper run on S under Builder PR #162: S has a different owner/branch/PR. Run a separately scoped **staging-provenance/whole-tree validator** checked against A and M, including copied blob identity and Manager-owned metadata. Verify safe read-only new workflow and original trust anchors unaffected. If a staged source edit/fixture/test/permission differs from A, STOP for separately scoped Builder correction and new audits; do not call it a byte-for-byte transplant.

G3. Establish FULL `test` on actual final stage HEAD S under the existing GitHub Actions required integration, not solely on A and not solely DOCS_ONLY. Authenticate the check-run's `head_sha=S`, app/integration, conclusion, PR identity and target base M. Stage must be current relative M (ancestor check) and have `mergeable_state` consistent with protected merge. A green GitHub merge preview, old source FULL, successful one-time helper, or a check-run on another SHA does NOT qualify.

G4. Independently audit exact S **without advancing master**: a fresh independent Auditor branch starts from separately verified canonical M, checks S's full source+metadata tree, history, permissions/credentials, exact source-blob custody, staging validator/exception contract, completeness of controls, original packet/TCW-050 evidence as supporting input and strict check. Auditor writes report/handoff on a DIFFERENT Auditor-only branch and opens its own evidence PR but does NOT merge it to master before source stage release. Manager independently reads full immutable Auditor report at its exact commit SHA, verifies its source-target S, Auditor branch creation/provenance, evidence-only diff and applicable CI. The independent report must explicitly say whether strict integration staging and S tree are accepted, without claiming installed live Actions L4. Any material stage S change, base M advance or failed check invalidates the release attempt and requires new target freeze/audit; do NOT re-label old PASS as new-head PASS. Auditor cannot approve owner release or merge source.

G5. Independently revalidate ALL live gates immediately before any proposed install: stage PR HEAD exact S, base M still equals live `master`, stage branch currently contains M, required same-stage-head `test` success, stage tree exact reviewed tree, unsigned/mutable comments not used as sole proof, clean four-file authorized source diff, source A unchanged, original source/machine baseline still authoritative, auditor evidence SHA exactly references S, ruleset unchanged, rollback operator present and protected merge method documented. If any discrepancy, STOP. Then request a **NEW separate explicit owner installation authorization naming stage PR and S, acknowledging real NEW read-only GitHub Actions workflow token and expected website master-push deployment/rollback**. Today's design/audit authorization is NOT this approval.

G6. Only after approved exact-stage release: Manager changes staged PR from DRAFT to ready, rechecks immutable S/M/CI/auditor/owner consent, guarded merges ONLY stage PR with exact expected head and existing GitHub protected PR requirements, no force/bypass/auto-merge. Independently fetch resulting master G, G's parents and tree; if actual G.tree differs from audited S.tree or master/ref changed unexpectedly, treat as installation incident, activate authorized forward rollback and bounded fresh audit. Do not force-rewrite master. GitHub may reject due required test/merge preview; STOP without weakening policy.

G7. Source is merely **INSTALLED**, NOT OPERATIONAL: verify actual FULL master `test`, normal website deployment and production verification on G; then integrate exact authenticated independent stage-audit evidence (the audit PR's docs-only merge) and Manager registry/task/closeout reconciliation AFTER stage merge with explicit `source=A`, `stage=S`, `integration=G` and exact success runs. Explicitly state that original Builder PR #162 was NOT merged; it remains historically identifiable and requires a separate Manager disposition, not fake merged status. The new readiness workflow may trigger from a later actual `.ai/shared/ACTIVE_TASKS.json`/Manager task master push; expected NO_ELIGIBLE_TASK is **not** a positive ready-task or operational PASS.

G8. Distinct L4 operational gate: safely run ACTUAL installed first-party new Actions on default branch with authorized disposable/non-product eligible checkpoint, no TCW-034 or trade PR #147 as a pilot; validate positive and negative canonical push / valid+invalid `workflow_dispatch`, real read-only GitHub token/PR/ref/branch/ancestor controls, exact original helper packet/hash, per-task/global PASS/FAIL/INFRA_ERROR/NO_ELIGIBLE_TASK and retained artifacts, no auto-freeze/merge. If dispatch/test tools are unavailable or credentials are not verifiable, report UNVERIFIED; keep original manual readiness as authority. Do not mark source TCW-047 CLOSED/operational before independent post-install evidence meets its accepted gates.

## 4. Pre-activation independent governance-audit questions and explicit STOP cases

The independently assigned Auditor must determine whether the proposed out-of-master premerge attestation can coexist with authoritative canonical V3.2 machine state without weakening exact-SHA freeze/role separation/closed-task evidence, or whether material validator/state revisions are mandatory. Require a machine-readable attestation schema, reject replay/stale/foreign-repo/wrong-PR/wrong-branch/mismatched tree/incorrect owner or signature, and identify who can change refs or write evidence before/after audit. No mutable PR comment, check-run URL or live ref alone can substitute an exact immutable audited commit. Verify a final S and its SHA need NOT be placed within its own tree; no hash circularity may be hand-waved away.

Independently test/argue whether strict GitHub protection applies to the actual stage PR head `test` in the specified method, including external Auditor report PR and premerge attestation ref having NO effect on master; stage head/preview required check state must be freshly fetched. Explain loss of protection if independently audited stage diff has added Manager control-plane paths: the staging audit must encompass ALL composite modifications, not merely the four old source blobs.

Identify any conflict with original TCW-047 expectation that automatic readiness is triggered by canonical registry/checkpoint changes, original helper target belongs to assigned Builder branch PR #162 and `worker_checkpoint_sha=A`, plus postmerge status transitions. A changed branch/ref/PR or updated task registry must not silently make legitimate historical source audit unverifiable or grant a staged Manager PR the Builder role. Any policy exception must explicitly limit applicability to a separately named Manager composite installation task; general relaxation of ordinary task audit freezes is OUT OF SCOPE.

Contested architecture must be rejected if: staging requires GitHub rule bypass; exact S is not independently audited; audit report must merge to master before S (making S stale); registry tries to embed S in S; a new unreviewed source SHA inherits TCW-050; source A differs in staged file bytes; original branch creation is overwritten by effective baseline; mutable comments alone assert authority; owner consent for prior SHA is reused; results called operational from non-installed/one-off tests; production rollback unavailable; or the normal repo workflow static validator would fail under the proposed exception with no independently audited bounded remediation.

## 5. Scope, task independence and current decision

This document is a **Manager-authored non-operative proposal** for a FRESH independent governance audit only. It is not a change to `.ai/shared/WORKFLOW_V3_2.md`, `scripts/audit-workflow.js`, `scripts/workflow-audit-readiness.js`, `.ai/shared/ACTIVE_TASKS.json` source task authority, active ruleset, Builder branch or source code. This proposal's documentary integration to master (after ordinary guarded PR/CI) is NOT installation or activation of the new exception. A distinct Manager post-audit acceptance/activation decision is required before any concrete governance implementation, and a separate owner approval before any staged source PR or installation.

The Auditor may verdict PASS for design consistency with REQUIRED CHANGES / PASS WITH NON-BLOCKING FINDINGS / FAIL — REMEDIATION REQUIRED within the canonical audit verdict vocabulary; a proposal-only PASS must never be represented as evidence the not-yet-created stage-head installation is executable or independently security-accepted. Require the Auditor to flag any unproved GitHub merge behavior or controls as unresolved test conditions.

Existing source TCW-047 remains OPEN/`WAITING_EXTERNAL_EVIDENCE`; its prior `USER_ACTION` gate for **design/audit commissioning only** is now satisfied by the owner's current explicit scoped consent, but separate future governance/installation authority remains unavailable. Original PR #162 DRAFT/UNMERGED, trade PR #147 DRAFT/UNMERGED at historical failed TCW-034 product `035c5f5112b7393f9d4f17685792548afa67dd2e`, TCW-034 manual readiness authoritative, TCW-035 inactive, historic Auditor PR #165 unmerged, no ESPN write or field validation changes.
