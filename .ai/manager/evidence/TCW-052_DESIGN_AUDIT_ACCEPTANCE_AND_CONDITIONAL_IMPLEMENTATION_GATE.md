# TCW-052 — Manager acceptance and separately gated material protocol implementation decision

Workflow V3.2 · Manager / Architect · 2026-09-20 America/New_York

**VERDICT: ACCEPTED — PASS WITH NON-BLOCKING FINDINGS, DOCUMENTARY DESIGN ONLY, exact TCW-051 proposal `c7b0923f5dfcf0a1e7ce5ac3b7d301b4de96cd75`.** This record does not activate any protected-release exception, grant staged source PR creation, change a GitHub branch protection rule, authorize installation, or establish first-party new readiness workflow L4. Product owner authorized ONLY design and this fresh independent governance audit. A NEW separate user authorization is required before a worker implements the material governance change, and further distinct approvals are required before staged-source PR creation and eventual exact-stage installation.

## 1. Independently verified source and Auditor custody

- Canonical pre-review master `a1108939b691db3c479268631e9047b0c3dd44bf`; source Manager design PR #177 documentary-only MERGED at exact frozen design HEAD `c7b0923f5dfcf0a1e7ce5ac3b7d301b4de96cd75`, original design branch creation `5b1259e350c5b35d97d6542784e1f97869f0ae83`, ONE source path `.ai/manager/evidence/TCW-051_PROTECTED_RELEASE_PROTOCOL_PROPOSAL.md`, exact Git blob `a38cc9aeccd7b7650cd7704b8788b8a96178db9b`. Documentation-only proposal PR #748/run `35490042285`/test `106023311829` SUCCESS, real postproposal master #749/run `35490067234`/test `106023373172` SUCCESS, deploy/production SKIPPED.
- Auditor PR #179 original historical branch creation/assignment SHA `3c93bac1e3d9fba0d5600e30f440757f2e27401a` preserved (zero initial changes then nonforce fast-forward to `a1108939b691db3c479268631e9047b0c3dd44bf`); independent Audit design frozen target exact `c7b0923f...`. PR #179 had TWO commits / only TWO authorized new Auditor evidence files `.ai/audit/TCW-052_PROTECTED_RELEASE_GOVERNANCE_AUDIT.md`, `.ai/auditor/TCW-052_HANDOFF.md`, exact Auditor HEAD `5c018fbbcf1365db7aff35ff037a89f8494fa8f5`, report Git blob `98cc8f05e988311c670e78085424e11d88101e53`, handoff blob `ba77e49e83db61e1f8ed71443d3965699269de57`. Manager directly read full independent report and handoff and independently verified GitHub PR diff, live registry/ruleset/source proposal, CI job steps and identity.
- Auditor exact-final-head DOCS_ONLY #752/run `35510497545`/test `106077434997` SUCCESS, workflow static audit/classifier/CI artifact SUCCESS; full app tests SKIPPED legitimately, so do not claim full implementation validation. Manager explicit acceptance published in Auditor PR #179 comment `5749793720`. Guarded PR #179 merge using exact expected Auditor HEAD succeeded at evidence-only integration master `6728e67c8d86ed3d4634f930097c006504d92b57`. Independently verified real postmerge master PUSH #753/run `35510697015`/test job `106077970764` SUCCESS, deploy/verify-production SKIPPED for Markdown-only evidence. No Builder/source/workflow code was included in either evidence PR.

## 2. Accepted findings — different severity for design and later actual activation

**TCW-052-F01 — LOW / NON-BLOCKING only for non-operative documentary design.** Auditor found no executable versioned immutable composite staging attestation, release-attempt nonce/replay and consumed/aborted ledger, independently authenticated Manager and owner approval/actor/signer policy, ref mutation custody, A(source)→S(stage)→G(merge) blob/provenance binding, validator/state-machine or negative fixtures. These are **HARD BLOCKING REQUIRED DELIVERABLES for future material governance IMPLEMENTATION/ACTIVATION**. An exact Git SHA authenticates bytes but not signer/actor or chat-role independence. Never accept mutable PR comments, branch names, or a user-provided SHA as sole evidence, and never weaken ordinary TCW_TASK_V2 independent freezes.

**TCW-052-F02 — LOW / NON-BLOCKING only for non-operative documentary design.** Proposal requires G.tree == independently audited S.tree but the live GitHub ruleset permits several merge methods. Any future executable protocol must expressly use only a verified two-parent `merge`, checking actual protected G parent1 == frozen exact M, parent2 == independently audited S, G.tree == S.tree, and master ref == G, plus real final-stage HEAD `test` and GitHub synthetic test-merge required-check semantics. The existing ruleset permitting squash/rebase does not authorize using them for this exact provenance protocol. A connector `expected_head_sha` guard is not an atomic base-M CAS: ref/ruleset/race rechecks and strict protection remain mandatory. Genuine GitHub S/G behavior can be proven only on a subsequently authorized stage, not in this documentary audit.

The design is a conditional architecture worth considering for separate implementation. **Its ACCEPTED verdict is not evidence that the proposed exception is currently compatible with live validators, that stage S or G exists, that the strict up-to-date problem is already solved, or that live first-party Actions L4 is established.** TCW-052's own report explicitly records the standalone branch-protection endpoint connector 403; active ruleset #22309639 was retrieved, strict up-to-date `test` integration ID 15368 and current user bypass NEVER. Re-fetch all applicable protections before any future actual release.

## 3. Manager commissioning decision and proposed next bounded task (NOT activated)

**DECISION: PROPOSE commissioning a separately numbered narrowly scoped MATERIAL GOVERNANCE IMPLEMENTATION, provisionally `TCW-053`, and a subsequent FRESH independent audit of its exact final implementation/registry/validator SHA. Do not activate or execute that implementation under the owner's current DESIGN+AUDIT-ONLY permission.** Future task number, branch and immutable activation master must be resolved from live GitHub at the separate authorization time; this record does not reserve a Builder branch, claim an active TCW-053 in the canonical registry, or delegate work to Builder. Do not create a staged-source branch or PR as part of governance implementation.

Proposed implementation deliverables and acceptance criteria, only IF separately owner-authorized:
1. A bounded Workflow V3.2/TCW_TASK_V2 policy amendment (or separately scoped companion protocol) applying ONLY to a clearly identifiable Manager-owned composite release task; original Builder branch/PR/head/original readiness preflight and ordinary frozen Auditor task contracts remain unchanged. Explicit opt-in and independently verified authorizations, never a global bypass.
2. Versioned `TCW_COMPOSITE_RELEASE_ATTESTATION_V1` machine-readable schema with exact repo ID/full name, M SHA/tree, source A immutable task/branch/PR/history/effective baseline and four path/mode/blob tuples, stage PR/ref/S SHA/tree/full diff, original A helper packet digest, source-A bounded auditor evidence, exact app/integration `test` check-run/job/preview identity, separate independently authored Auditor report/head/actual exact-S verdict and strict required ruleset digest, owner staging-plan and later separate exact-S release approvals, rollback operator and release-attempt nonce/expiry.
3. Authenticate actual Manager/owner/Auditor authority and GitHub permission/signer constraints rather than inferring independence from same-account chat roles; immutable evidence commit/blob retrieval and branch-ref/HEAD integrity, reject foreign repo/wrong PR/ref/moved evidence/forged author/old authority and stale/consumed/aborted attempts, persist consumed tuple after actual G. Premerge S/audit evidence remains off-master without falsely embedding S into S's own tree. No mutable PR comment as unique trust anchor.
4. A fail-closed state machine and source-vs-composite scope validator rejecting replay, missing/changed parent/base/tree, unexpected paths, A/S byte mismatch, changed GitHub ruleset/required `test` app/preview status, non-FULL exact-stage CI, Auditor verdict not for exact S, owner consent not for exact S or absent rollback. Restrict actually executed merge method to proven two-parent `merge` and assert parent1 M/parent2 S/G.tree S.tree/master ref G after normal protected merge. Distinguish premerge checked assumptions from real later GitHub runtime evidence.
5. Bounded automated positive/negative fixtures for the above, including exact-head/master drift, test on older source A or stale S1, unexpected extra staged Manager paths, changed HEAD S2, forged/moved evidence, duplicate nonce, missing owner authority, alternate squash/rebase, required check from wrong app, old original-helper result misused as staging PASS, immutable original branch-creation vs later effective baseline confusion, real L4 unverified status.
6. A formally frozen implementation SHA, required FULL CI, separately executed material workflow/control-plane **FRESH Independent Auditor** task/branch/report and exact-final-head CI, Manager acceptance and guarded integration of the governance policy/validator only if audited PASS and separate owner scope permits. No proposed readiness Actions installation or staging PR during that governance implementation. A future staging-source PR requires an additional explicit owner approval; a later independently audited exact S requires another distinct explicit installation authorization, strict up-to-date FULL `test` and supervised rollback. Real post-install master FULL/deploy/production plus newly installed automatic workflow master-push/valid-invalid dispatch/token/original packet/retained PASS/FAIL/INFRA_ERROR/NO_ELIGIBLE_TASK L4 must follow before operational acceptance.

Any inability to satisfy authenticated authority separation, preserve current ordinary task validation or reproduce strict GitHub stage HEAD + merge-preview/parentage semantics is a future blocking implementation finding requiring independent redesign. Do not silently weaken branch protection or claim successful installation based on a design-only verdict.

## 4. Current user action and protected neighboring lanes

Current TCW-051 documentary design is independently accepted and can close as DESIGN ONLY after explicit machine `VERIFYING_MASTER` audit acceptance and successful exact evidence-only integration/master CI. TCW-052 separate audit-only task can close with `audit_verdict:NOT_APPLICABLE` for audit-of-audit and no canary, with exact evidence CI/merge/master success. Source TCW-047 remains OPEN / NOT INSTALLED with TCW-050 bounded PREINSTALL L1–L3 PASS only on immutable Builder A `17e5f413f2afd3d743fd28d401f0df421825df2a`; owner design-audit permission is now fully consumed. Until a NEW express owner action, mark source `WAITING_EXTERNAL_EVIDENCE`/`USER_ACTION` for whether to commission this proposed limited material governance implementation. **Not approval to implement, stage, merge, change protection or install.**

Builder PR #162 remains DRAFT/UNMERGED/BEHIND protected master at A. Original actual Builder creation `7ca2953009d37a014e041cc24f4934bfe61b5cad`, effective synchronized verification `e0fe6309dc0aaa184bbeef35861f7d49256385b7`, original TCW-048 FAILED, prior TCW-049 old-source PASS and accepted TCW-050 exact-A PASS remain separate immutable evidence. Trade Builder PR #147 DRAFT/UNMERGED at TCW-034 original product target `035c5f5112b7393f9d4f17685792548afa67dd2e`, manual TCW-034 readiness and TCW-035 inactivity unchanged. No new workflow, GitHub ruleset, product, ESPN, field validation, token, external account or deployment changes are authorized by this documentary Manager decision.
