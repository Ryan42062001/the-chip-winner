# TCW-026 — Workflow V3.2 Cross-Project Parity Upgrade

Schema: TCW_TASK_V2
ROLE ROUTING: Manager / Architect
STATUS: MERGE_READY
DEPENDENCY: INDEPENDENT
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FULL_REFRESH
REFRESH REASON: Product owner explicitly requested reconciliation and implementation of applicable workflow/control-plane upgrades from The War Room and Family Finance Hub.
ACTIVATION: USER_AUTHORIZED
BLOCKER TYPE: NONE
USER ACTION REQUIRED: false
BLOCKED ON: NONE
NEXT OWNER: Independent Auditor / QA after Manager integration

## AUTHORIZED BASELINE

Canonical implementation base:
`7bb690429ad5b829e36e5d464ae9d7e74cc77ce0`

Branch:
`manager/tcw-026-workflow-v32-parity-upgrade`

This base already contains Manager-integrated and production-verified TCW-025 remediation. TCW-025's independent findings retest remains separately required and must not be waived by this workflow task.

## OBJECTIVE

Port every mature cross-project workflow/control-plane improvement from The War Room and Family Finance Hub that materially applies to The Chip Winner, while preserving The Chip Winner's smaller in-season team, read-only product boundary, current field-validation authority, and active product gates.

## REQUIRED UPGRADES

1. Exactly two forward-looking execution modes:
   - `STANDARD_CHAT_HIGH` default;
   - `WORK_MODE` only for substantial autonomous execution leverage.
2. `FAST_REFRESH` default, documented `FULL_REFRESH` exception with reason, and `BOUNDED_REMEDIATION_REFRESH` for accepted same-task findings.
3. Active-only machine registry schema v3 with blocker/user-action metadata, refresh metadata, explicit write scopes, audit/canary requirements, worker-slot identity, and stronger collision/dependency validation.
4. Manager Integration Record and TCW_TASK_V2 template for new tasks.
5. Same-role concurrency with fail-closed branch/PR/worker-slot/write-prefix collision detection.
6. Manager execution packets and accepted-decision consumption.
7. Mechanical audit-readiness preflight and frozen audit packet template.
8. Dry-run-by-default Manager transition helper with rollback on static-state failure.
9. Derived user-action queue.
10. Manager integration queue and known-CI-debt registry.
11. Full-workforce six-row Next Activation dashboard and compact handoff standard.
12. Standing Manager authority for bounded workflow improvements within protected boundaries.
13. Material workflow/control-plane independent audit gate.
14. Fail-closed documentation-only CI fast path that preserves the always-present `Deploy website / test` job/check and full validation for every ambiguous/non-doc/master/manual case.
15. Durable CI evidence artifacts with classification, exact SHA/range, changed paths, stage outcomes, predecessor continuity, and captured logs.

## CI FAST-PATH CONTRACT

Only these PR paths may classify `DOCS_ONLY`:
- root-level `*.md`;
- `.ai/**/*.md`;
- `docs/**/*.md`.

Everything else is FULL, including:
- `.ai` JSON/machine-state;
- workflows;
- package/lock files;
- scripts;
- source;
- tests;
- config;
- mixed changes;
- deletions/renames involving non-doc paths;
- malformed/empty/unresolvable evidence;
- unsupported PR actions;
- all pushes;
- manual dispatch.

A docs-only synchronize shortcut requires a successful same-PR predecessor `Deploy website` run at the immediately preceding head. If continuity is unavailable, CI falls back to FULL rather than trusting the shortcut.

The `test` job always exists.

## NON-GOALS

Do not import:
- War Room protected historical-scoring authority/consumption receipts;
- War Room draft-specific model/custody semantics;
- Family Finance Hub monetary reconciliation/policy controls;
- FFH Supabase/runtime rules;
- any workflow rule that presumes a role The Chip Winner does not have.

Do not modify:
- in-season recommendation policy;
- ESPN provider/data semantics;
- Trade Analyzer behavior;
- `config/field-validation.json`;
- FV-SEASON-01 status;
- production fantasy-football behavior.

## OWNED FILES / SYSTEMS

Allowed:
- `.ai/shared/**`;
- `.ai/manager/**`;
- `.ai/roles/**`;
- `.ai/audit/**`;
- `.github/workflows/deploy-pages.yml`;
- workflow/CI helper scripts;
- CI-classifier test;
- `package.json`.

Forbidden:
- `src/**`;
- `config/**`;
- Builder/Strategy/R&D/Auditor task evidence not explicitly part of the Manager control-plane migration;
- product-specific accessibility/mobile remediation;
- external data/provider code.

## MANAGER INTEGRATION RECORD

PRODUCTION_SHA: N/A — workflow/control-plane/CI tooling task; no fantasy-football behavior change
VALIDATED_CI: PR workflow #574 / run 35417946775 PASS in FULL mode on exact executable candidate head 00516fa9001bc97cfcc241357333d77fad9eb217
HANDOFF_SHA: 00516fa9001bc97cfcc241357333d77fad9eb217 — exact executable candidate checkpoint before merge-readiness metadata
INTEGRATION_SHA: Not yet established
MANAGER_VERDICT: MERGE_READY — exact-head final validation still required
AUDIT_STATUS: REQUIRED — fresh independent workflow/control-plane audit after integration

## KNOWN CI OWNERSHIP

- Task-owned: workflow validator, CI classifier, workflow YAML, routing metadata, helper scripts.
- Inherited: NONE known.

## ACCEPTANCE CRITERIA

- All applicable upgrades above are implemented consistently.
- Legacy current-task execution-mode metadata is normalized without rewriting closed historical task evidence.
- Static validation detects unsafe active-state contradictions/collisions.
- CI classifier unit tests pass.
- Exact-head PR uses FULL validation because this task changes workflow/scripts/package/machine-state files.
- Full existing product/model/browser/a11y/mobile/security gates remain green.
- Master post-merge full CI, Pages deployment, and production smoke pass because workflow/scripts are deployable changes.
- No product/Strategy/field scope changes.
- Manager freezes the exact integrated target and routes one fresh independent workflow/control-plane audit.
- TCW-026 may not become CLOSED before that independent audit passes or returns only accepted non-blocking findings.

## NEXT ACTION

Complete implementation on the assigned Manager branch, run exact-head CI, inspect the complete diff, merge only after the Manager integration gate, verify master deployment/production, then route the fresh control-plane audit.
