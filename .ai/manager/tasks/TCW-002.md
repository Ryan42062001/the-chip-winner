# TCW-002 — Independent Release 1.0 Baseline Audit

ROLE ROUTING: Independent Auditor / QA
STATUS: AUTHORIZED AFTER TCW-001 CLOSEOUT MERGES

## TASK ID

TCW-002

## OBJECTIVE

Independently verify the current Release 1.0 baseline and canonical `.ai` state against actual repository, branch/PR, CI, and field-registry evidence. Produce a PASS / PASS WITH NON-BLOCKING FINDINGS / FAIL verdict for the claimed current project state and identify any blocking discrepancies.

## WHY IT MATTERS

The new canonical workflow should not become trusted merely because Manager created it. Independent verification is needed before future work relies on the reconstructed milestone, completed-scope claims, field-gate counts, and protected-workflow assumptions.

## VERIFIED STARTING STATE

Refresh from repository before acting. Expected starting point after TCW-001 closeout:

- repository `Ryan42062001/the-chip-winner`;
- protected default branch `master`;
- package version expected `0.9.88` unless repository evidence has advanced;
- canonical `.ai/shared/*` files present;
- Release 1.0 remains evidence-gated by `config/field-validation.json`;
- Waiver Engine v2 and reviewed Season/Playoff Intelligence scope are documented as complete;
- ESPN write actions remain outside Release 1.0.

Do not trust these expectations if current repository evidence differs.

## REQUIRED BEHAVIOR

- Verify current `master` checkpoint and package version.
- Verify open PR/branch state relevant to active work.
- Verify the Release 1.0 field registry statuses directly from `config/field-validation.json`.
- Verify that canonical project-state/roadmap claims do not contradict stronger repository evidence.
- Verify TCW-001 bootstrap changed only coordination/docs files and did not alter production behavior.
- Verify the protected CI/release workflow actually exists and matches the canonical description.
- Review the recorded discrepancies concerning `docs/next-codex-task.md`, historical `docs/roadmap.md` wording, and field-registry `baselineVersion` without silently resolving them.
- Preserve independent audit authority; do not change Manager conclusions merely for consistency.

## LIKELY COMPONENTS

- `.ai/shared/*`
- `.ai/manager/*`
- `AGENTS.md`
- `package.json`
- `docs/roadmap.md`
- `docs/field-validation.md`
- `config/field-validation.json`
- `.github/workflows/deploy-pages.yml`
- relevant commits / PRs / workflow runs

## NON-GOALS

- No production implementation.
- Do not mark any real-world field check passed from synthetic tests.
- Do not alter product requirements or architecture.
- Do not create a successor milestone.

## EDGE CASES

- If repository state advanced after assignment creation, audit the newer state and explicitly record the discrepancy.
- If canonical `.ai` claims and older documents disagree, identify both claims and prefer the newest verified evidence rather than silently rewriting history.
- If CI evidence is unavailable, state that it is not verified.

## TEST REQUIREMENTS

No new tests are required unless the audit reproduces a deterministic defect. Verify available repository CI/test evidence and inspect changed-file scope for TCW-001.

## REQUIRED VALIDATION LEVEL

Independent repository/CI audit. Field checks that require real-world evidence remain outside the audit's power to pass unless such evidence already exists in the repository.

## ACCEPTANCE CRITERIA

- Current checkpoint, version, field-status counts, active milestone, and relevant branch/PR state are independently verified.
- TCW-001 production-behavior non-change claim is verified from changed-file scope.
- Canonical claims are checked against stronger repository evidence.
- Every material discrepancy is classified blocking or non-blocking.
- Final verdict is exactly PASS, PASS WITH NON-BLOCKING FINDINGS, or FAIL with rationale.

## EXPECTED HANDOFF

Update `.ai/auditor/HANDOFF.md` with the standard HANDOFF fields plus the independent verdict. Write only within `.ai/auditor/` unless Manager explicitly authorizes another path.
