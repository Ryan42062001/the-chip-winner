# TCW-059 — Protected Release External Identity + Ledger Feasibility Research

Schema: TCW_TASK_V2
ROLE ROUTING: Research & Development
STATUS: ASSIGNED
DEPENDENCY: INDEPENDENT
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
REFRESH REASON: N/A
ACTIVATION: RESEARCH ONLY — resolve external identity, publisher-rights, protected nonce/consumption ledger, retained packet custody and rollback proof architecture before any TCW-047 staging-source authorization
BLOCKER TYPE: NONE
USER ACTION REQUIRED: false
BLOCKED ON: NONE
NEXT OWNER: Research & Development — perform bounded current-source feasibility research and return explicit protocol verdict to Manager; no staging source, workflow installation, permission mutation or release action

## AUTHORIZED BASELINE

Repository:
`Ryan42062001/the-chip-winner`

Canonical assignment master:
`fe3ca66c8905afea8302ae6d6aca0ae7ffeb0952`

Assigned branch:
`rnd/tcw-059-protected-release-external-evidence-research`

The R&D branch was created from the exact canonical assignment master above and must preserve that historical creation checkpoint. Fast Refresh against live canonical state is required before research writes.

Relevant accepted upstream state:
- TCW-047 original Builder PR #162 remains DRAFT / OPEN / UNMERGED at exact audited source `17e5f413f2afd3d743fd28d401f0df421825df2a`.
- Exact TCW-047 effective source scope from `e0fe6309dc0aaa184bbeef35861f7d49256385b7` to source A is exactly four authorized files.
- Protect Master ruleset #22309639 is active; strict required GitHub Actions `test` from integration ID 15368; no bypass actors; current user cannot bypass.
- TCW-050 bounded pre-install source/security PASS remains exact-A only and does not establish installed Actions L4 or a current mergeable source.
- TCW-053 dormant composite protected-release governance source was integrated and independently audited; historical TCW-053 is CLOSED.
- TCW-053 contract remains DORMANT / RELEASE_HOLD unless real external identity/publication rights, durable ledger, exact staging/audit/owner authority and rollback proof can be independently established.
- Current connected GitHub user is `Ryan42062001` / actor ID `312284033`, repository permission `admin`. This single actor identity must NOT be treated as proof of the contract's required distinct Owner/Manager/Auditor publisher identities.

## OBJECTIVE

Determine whether a **real, independently verifiable, least-privilege external evidence architecture** can satisfy the dormant TCW-053 protected-release contract for a future TCW-047 staged source attempt without weakening Workflow V3.2, GitHub branch protection, role separation or release authority.

This is a research/design-feasibility task only. It must not create a staging branch/PR, install the TCW-047 Actions workflow, write a protected ledger, alter permissions/rulesets, create credentials, dispatch a workflow, merge any source PR or claim release readiness.

## REQUIRED RESEARCH

### 1. Distinct authenticated GitHub actor / publisher architecture

Research practical options for satisfying the contract's distinct Owner / Manager / Auditor actor and evidence-publication requirements.

For each serious architecture document:
- exact GitHub identity type: human account, GitHub App installation/bot, Actions app, deploy key or other supported identity;
- whether GitHub exposes a stable independently fetchable actor ID;
- repository permission level and exact least privileges needed;
- how Owner approval, Manager stage authority and Auditor evidence publication remain distinct;
- whether the proposed identities can publish immutable evidence without sharing one credential or impersonating another role;
- how evidence can be independently verified from GitHub APIs after publication;
- credential custody / rotation / revocation / compromise implications;
- whether the architecture is supported by current official GitHub behavior rather than inferred from chat roles.

Do not count the same `Ryan42062001` account acting under different ChatGPT role labels as distinct actors.

### 2. Protected durable nonce / consumption ledger

Research a real append-only or effectively irreversible ledger design able to support:
`PREPARED -> REVIEWED -> OWNER_APPROVED -> RELEASED | ABORTED`

It must support:
- high-entropy per-attempt nonce;
- immutable tuple digest binding;
- finite expiry/freshness;
- independently verifiable before/after state;
- irreversible consumed/aborted semantics;
- append-only history or equivalent tamper-evident chain;
- protected write authority separated from ordinary Builder source;
- safe concurrent-attempt handling;
- replay rejection;
- verifiable rollback/incident behavior;
- no dependence on mutable PR comments or user-supplied JSON as authority.

Evaluate GitHub-native options first where possible, but do not assume repository commits/branches are sufficiently protected without analyzing who can write/rewrite them and how verification works.

### 3. Retained original TCW-047 packet-byte custody

The contract currently records the historical packet SHA256 and artifact ID but explicitly says retained packet archive BYTES are not independently authenticated.

Research a feasible evidence chain that can prove:
- exact retained artifact bytes;
- original packet SHA256;
- source A / task / run / job binding;
- artifact provenance and retention limitations;
- durable archival strategy if GitHub Actions artifact retention is insufficient;
- independently verifiable custody without exposing secrets/private tokens.

State clearly whether the existing historical artifact can still be fetched and independently verified now, or whether a future re-created packet must be treated as a distinct artifact rather than pretending to recover unavailable historical bytes.

### 4. Owner stage-plan and exact-S installation approvals

Define an independently verifiable evidence mechanism for the two deliberately separate owner decisions:
1. authorization to create one exact Manager-owned staged source attempt;
2. later authorization to install/merge that exact independently audited S.

The mechanism must bind repository, attempt/nonce, frozen M, stage PR/branch/exact S, audit target/verdict, rollback operator and expiry. It must not rely on ambiguous free-form chat alone as the durable proof consumed by the release validator.

Research how an owner can approve through a protected GitHub-native or otherwise verifiable channel without granting unrelated write authority.

### 5. Independent exact-S Auditor publication

Research how a future independent Auditor can publish an exact-S whole-tree verdict off master without:
- advancing master and making strict S stale;
- writing to stage S;
- using the same authenticated publisher identity as Owner or Manager;
- creating a self-referential stage SHA.

Specify immutable commit/ref/blob custody and how Manager can fetch/verify it before merge.

### 6. Rollback operator / incident evidence

Research what must be proven before a protected release attempt can name a rollback operator:
- identity and actual repository rights;
- exact rollback plan;
- restrictions imposed by no-force / pull-request-only master protection;
- evidence needed to show rollback is operationally possible without bypass;
- incident path if actual G differs from expected two-parent merge/tree.

### 7. Current GitHub rule compatibility

Independently verify current ruleset #22309639 and analyze the dormant contract's stricter `merge`-only requirement against GitHub's presently allowed merge/squash/rebase methods.

Research must distinguish:
- repository ruleset capability;
- Manager procedural restriction;
- any required future ruleset change;
- whether changing allowed merge methods is necessary, optional or forbidden for the proposed protocol.

No ruleset change is authorized by TCW-059.

## REQUIRED VERDICT

Return exactly one top-level R&D verdict:

- `PROTOCOL_FEASIBLE_WITH_SEPARATE_OWNER_ACTIONS` — a concrete supported architecture exists and can be independently verified without weakening current protections; identify every external setup/owner action still required before staging.
- `MANAGER_DECISION_REQUIRED` — multiple bounded architectures are technically viable but policy/security/cost/operational tradeoffs require Manager selection before any setup.
- `NO_SAFE_PROTOCOL_FOUND` — no researched architecture can satisfy the accepted contract without weakening a material gate or introducing unverifiable authority.
- `STALLED_EXTERNAL_ACCESS_REQUIRED` — a key claim cannot be resolved without access the R&D lane does not have; name the exact missing evidence and why it cannot be inferred.

A verdict is research evidence only. It never authorizes staging, credential creation, ledger writes, workflow installation, ruleset mutation, merge or release.

## EVIDENCE DISCIPLINE

Use:
- VERIFIED FACT
- STRONG EVIDENCE
- INFERENCE
- SPECULATION
- UNKNOWN

For external platform behavior additionally classify:
- OFFICIALLY SUPPORTED
- OBSERVED AND REPRODUCIBLE
- OBSERVED BUT FRAGILE
- INFERRED
- UNKNOWN

Prefer current official GitHub documentation and actual read-only repository observations. Distinguish what the connected GitHub App can read/write from what a future production identity would require.

## DELIVERABLE

Create:
`.ai/rnd/TCW-059_PROTECTED_RELEASE_EXTERNAL_EVIDENCE_RESEARCH.md`

Update:
`.ai/rnd/HANDOFF.md`

The research artifact must include:
- current verified repository/ruleset/source state;
- actor/publisher architecture options and recommended least-privilege mapping;
- ledger options with threat model and durability analysis;
- retained packet-byte custody conclusion;
- owner approval evidence design;
- exact-S Auditor publication design;
- rollback proof design;
- explicit ruleset compatibility analysis;
- attack/failure cases: replay, stale evidence, identity reuse, moved refs, ledger rewrite, lost artifact, revoked rights, concurrent attempts, stale owner consent, changed S/M, unexpected merge method;
- exact external setup actions required, clearly separated from actions already authorized;
- explicit verdict from the required taxonomy;
- Manager decision points;
- recommendation whether Manager may later ask the product owner for a **specific** staging authorization, or must stop.

## OWNED FILES / SYSTEMS

Allowed path prefixes:
- `.ai/rnd/TCW-059_PROTECTED_RELEASE_EXTERNAL_EVIDENCE_RESEARCH.md`
- `.ai/rnd/HANDOFF.md`

Forbidden path prefixes:
- `.ai/shared/`
- `.ai/manager/`
- `.ai/roles/`
- `.ai/strategy/`
- `.ai/audit/`
- `.ai/auditor/`
- `.ai/builder/`
- `.github/`
- `src/`
- `test/`
- `scripts/`
- `config/`
- `extensions/`
- `package.json`
- `package-lock.json`

## NON-GOALS / HARD HOLDS

TCW-059 may NOT:
- create or modify the TCW-047 staging source branch or PR;
- modify Builder PR #162 or Builder source A;
- create Owner/Manager/Auditor GitHub accounts, Apps, credentials or tokens;
- write any nonce/ledger/approval record;
- change branch protection or ruleset #22309639;
- install/dispatch the TCW-047 workflow;
- merge any source, audit or control-plane PR;
- authorize itself or TCW-047 for release;
- modify product/ESPN/trade code or data;
- claim distinct role identities from one GitHub account.

## MANAGER INTEGRATION RECORD

PRODUCTION_SHA: N/A — research only
VALIDATED_CI: PENDING — exact final R&D PR head required
HANDOFF_SHA: PENDING
INTEGRATION_SHA: PENDING
MANAGER_VERDICT: PENDING — Manager consumes R&D verdict; research does not authorize staging
AUDIT_STATUS: NOT_APPLICABLE — research only; any material governance/source implementation or operational release target requires its own fresh independent audit

## KNOWN CI OWNERSHIP

- Task-owned: research artifact and R&D handoff only.
- Inherited: no production or workflow CI result may be represented as TCW-059 evidence.
- R&D PR should be documentation-only and must pass the exact final-head required repository check.

## ACCEPTANCE CRITERIA

R&D is Manager-review-ready only when:
- current protected master/ruleset/source identities are refreshed rather than copied blindly;
- actor/publisher separation is resolved to concrete supported options or explicitly found infeasible;
- ledger durability and write-authority boundaries are threat-modeled;
- packet-byte custody is resolved without fabricating historical bytes;
- owner plan vs exact-S installation approvals remain distinct;
- independent Auditor publication can remain off-master without making S stale;
- rollback proof is operationally coherent under protected master;
- no external setup or write action is performed;
- the required verdict is explicit;
- only two authorized R&D paths change;
- one R&D PR exists;
- exact final-head CI passes;
- R&D stops and returns to Manager without merge.

## VALIDATION PLAN

Use Fast Refresh from the canonical assignment master and live protected repository state. Read Workflow V3.2, TCW-047, closed TCW-053 evidence, `scripts/workflow-composite-release.js`, R&D charter/handoff, current Protect Master ruleset and only additional repository evidence needed.

Use current web research for GitHub identity/App/permissions/ruleset/artifact/attestation behavior because those platform capabilities are time-sensitive. Prefer official GitHub documentation and cite it in the artifact.

Open one R&D PR and return exact branch/head/PR/CI plus the top-level verdict to Manager. Do not merge.
