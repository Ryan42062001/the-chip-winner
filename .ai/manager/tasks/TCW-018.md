# TCW-018 — FV-ESPN-05 Real Game-Lock / Availability Transition Field Retest

Status: ASSIGNED — POST-REMEDIATION RETEST
Owner: Independent Auditor / QA
Execution mode: STANDARD_CHAT
Dependency: INDEPENDENT
Assignment production baseline: `b6e6a2dabb0e2d9e404704d7e8997110ce403060`
Expected task branch: `auditor/tcw-018-lock-post-remediation`
Merge authority: Manager

## Objective
Independently retest the accepted TCW-018 lock-transition defect after deployed TCW-020 remediation and determine whether `FV-ESPN-05 — Authenticated lock and availability transitions` can now become a PASS CANDIDATE.

## Existing real field evidence
Privacy-safe Manager intake remains:

`.ai/manager/evidence/TCW-018_LOCK_FIELD_INTAKE.md`

The original real transition established:
- pre-lock ESPN showed James Cook III with `MOVE` available and a 1:00 PM game;
- deployed pre-lock Lineup Lab showed Cook 15.9 vs Ashton Jeanty 17.9 with a projection lean toward Jeanty;
- after kickoff ESPN showed Cook in an active first-quarter game state with the `MOVE` control gone;
- no roster transaction or simulated lock state was used;
- a successful post-lock deployed refresh restored `Live ESPN snapshot`;
- the complete-lineup optimizer respected seven roster locks and returned no change recommended;
- the separate START / SIT comparison still rendered the same unqualified projection lean after Cook had locked.

Independent Auditor PR #93 returned `FAIL — REPRODUCED DEFECT` with accepted finding:

`TCW-018-F01 — MEDIUM — BLOCKING`

## Accepted remediation
TCW-020 — START/SIT Lock-Awareness Remediation was accepted and merged through Builder PR #95.

Verified production integration:
- Builder implementation checkpoint: `6804c8c0a1b55f5959daab8635fa8d71d6a43f73`;
- final Builder handoff checkpoint: `545bacc25a49a71179335323d6b1befc01562149`;
- Manager merge-ready PR head: `61ade8aec1b9db28468f61ad698954d48c35d3b2`;
- merged `master`: `b6e6a2dabb0e2d9e404704d7e8997110ce403060`;
- exact-head PR workflow #508: PASS;
- post-merge master workflow #509: test PASS, GitHub Pages deploy PASS, production smoke PASS.

The deployed remediation reuses the complete-lineup optimizer's existing `getLineupLockReason()` semantics. When either selected comparison player is explicitly locked by ESPN or kickoff has passed, the START / SIT surface is expected to become clearly non-actionable/informational and must not present an unqualified actionable `PROJECTION LEAN`.

`config/field-validation.json` remains unchanged and `FV-ESPN-05` remains pending.

## Post-remediation field contract
Use the smallest genuine deployed evidence necessary to determine whether TCW-018-F01 is fixed.

Prefer reusing the existing real pre-lock/transition evidence where it remains relevant. Do not manufacture another roster move, lock state, injury state, or availability transition.

For the post-remediation observation, verify against a naturally locked/post-kickoff roster player on the deployed site that:
1. Refresh ESPN reaches the final valid current state, or any failure state is handled truthfully before the comparison is judged;
2. Lineup Lab recognizes the real lock/post-kickoff state;
3. the START / SIT comparison involving the locked player no longer presents an unqualified actionable preference;
4. any remaining ESPN/FantasyPros projection comparison is clearly informational/non-actionable and preserves source separation;
5. normal unlocked comparison behavior is not claimed from field evidence unless actually observed.

If the same real locked state is still naturally observable, use it. If no genuine locked state is currently observable, do not simulate one; return `INCONCLUSIVE` or wait for the next natural opportunity as appropriate under the evidence contract.

## Auditor disposition
Return exactly one:
- `PASS CANDIDATE`
- `FAIL — REPRODUCED DEFECT`
- `INCONCLUSIVE`

A PASS CANDIDATE must be independently supported by real deployed evidence that the previously reproduced stale actionable START / SIT behavior no longer occurs for the observed locked state.

A FAIL must identify the smallest deterministic reproduced defect.

An INCONCLUSIVE verdict must identify only the genuinely missing observation and must not infer unobserved behavior.

## Privacy
Do not commit raw screen recordings. Persist only privacy-safe observations required for the verdict.

## Next gate
Independent Auditor post-remediation field retest -> Auditor PR/handoff -> Manager acceptance. `FV-ESPN-05` may change from pending only through a separate bounded Manager integration after an accepted PASS CANDIDATE.
