# TCW-018 — FV-ESPN-05 Real Game-Lock / Availability Transition Field Retest

Status: BLOCKED — REMEDIATION REQUIRED
Owner: Independent Auditor / QA
Execution mode: STANDARD_CHAT
Dependency: HARD on TCW-020
Accepted verdict merge: `f9029d8eaa603bdcb039492a554fcd699948fa29`
Expected task branch: `auditor/tcw-018-lock-field-retest`
Merge authority: Manager

## Objective
Independently evaluate one real authenticated ESPN game-lock transition across deployed refreshes, verifying that The Chip Winner revalidates or obsoletes prior advice rather than continuing to present stale actionable guidance.

## Field contract
Authoritative field item: `FV-ESPN-05 — Authenticated lock and availability transitions`.

The field contract requires a real player to be observed before and after a genuine lock/availability transition, deployed ESPN refreshes around that transition, and enough repeated recommendation context to determine whether prior advice was revalidated, withdrawn, marked unavailable, or otherwise made non-actionable when required. No state may be manufactured or simulated.

## Received real evidence
Privacy-safe Manager intake: `.ai/manager/evidence/TCW-018_LOCK_FIELD_INTAKE.md`.

Observed sequence:
- pre-lock ESPN showed James Cook III with `MOVE` available and a 1:00 PM game;
- deployed pre-lock Lineup Lab showed Cook 15.9 vs Ashton Jeanty 17.9 with a projection lean toward Jeanty while the optimizer did not identify an action clearing its threshold;
- after kickoff ESPN showed Cook in an active first-quarter game state with the `MOVE` control gone;
- one deployed post-lock refresh entered the retained-snapshot failure state;
- a follow-up refresh succeeded and restored `Live ESPN snapshot`;
- post-lock Lineup Lab said `7 roster locks respected because ESPN reported a lock or kickoff passed` and the optimizer returned `no change is recommended`;
- the generic Cook-vs-Jeanty START / SIT comparison remained viewable with the same projection lean after Cook had locked;
- no roster transaction or simulated lock state was used.

## Independent audit result
Independent Auditor PR #93 returned:

`FAIL — REPRODUCED DEFECT`

Accepted finding:

`TCW-018-F01 — MEDIUM — BLOCKING`

The complete-lineup optimizer correctly respected the genuine lock, but the separate START / SIT comparison remained selectable after the player locked and still rendered an unqualified `PROJECTION LEAN`. Static review confirmed that `compareRosterPlayers(...)` does not inspect roster-entry locks, player locks, or passed kickoff while the UI presents the output as start/sit decision guidance.

Manager independently verified the mismatch and accepted the finding. `FV-ESPN-05` remains pending.

## Remediation dependency
Builder remediation is tracked as:

`TCW-020 — START/SIT Lock-Awareness Remediation`

TCW-018 is blocked until TCW-020 is accepted, merged, and deployed/production-verified.

## Post-remediation retest
After TCW-020 deployment, reactivate a fresh Independent Auditor lane for TCW-018.

Preserve and reuse the existing real pre-lock/transition evidence where valid. Do not ask the product owner to manufacture another transition. The post-remediation retest should obtain only the smallest genuinely necessary deployed evidence showing that a naturally locked/post-kickoff player can no longer produce stale actionable START / SIT guidance. If that real state is no longer observable, wait for the next natural opportunity.

## Auditor disposition on retest
Return exactly one:
- `PASS CANDIDATE`
- `FAIL — REPRODUCED DEFECT`
- `INCONCLUSIVE`

## Privacy
Do not commit raw screen recordings. Persist only privacy-safe observations needed for the field verdict.

## Next gate
TCW-020 Builder remediation -> Manager review/integration -> deployed verification -> fresh TCW-018 Independent Auditor post-remediation field retest.
