# TCW-012 — Waiver Field Diagnostics Visibility

ROLE ROUTING: Implementation Engineer / Builder
STATUS: ASSIGNED
EXECUTION MODE: STANDARD_CHAT
DEPENDENCY: HARD on the existing FV-WAIVER-01 field requirement
AUDIT REQUIRED: No independent product audit required before merge; deployed real field retest remains required before FV-WAIVER-01 may pass.

## OBJECTIVE

Expose the waiver engine's existing future-discovery diagnostics in the deployed Waivers UI so the real FV-WAIVER-01 field run can record exhaustive candidate/scenario volume without changing waiver behavior.

## VERIFIED BASELINE

- Assignment `master`: `074e110e85189f4473502c1c7fa72a18d88a2a10`.
- Release 1.0 field gate: 7 passed / 6 pending.
- FV-WAIVER-01 remains pending.
- Real deployed user recording already confirms the Waivers page is responsive in the observed session, but the required enumeration diagnostics are not visible.
- `buildWaiverPriorityBoard()` already exposes `futureDiscovery` with `consideredAdds`, `completeAdds`, `scenarioCount`, and `qualifiedAdds`.
- The current UI only presents `qualifiedAdds`; no new enumeration logic is required.

## REQUIRED CHANGE

In the Waivers priority/transparency surface, when `board.futureDiscovery.status === "ready"`, display all four existing values clearly and together:

- considered adds — `consideredAdds`;
- complete adds — `completeAdds`;
- scenarios evaluated — `scenarioCount`;
- qualified adds — `qualifiedAdds`.

Use concise user-facing wording that makes the values easy to capture in a screen recording. Exact internal field names may also be included in accessible/data markup if useful, but the visible text should be understandable without developer tools.

When discovery is not `ready`, preserve the existing status/reason semantics. Do not present zero-valued diagnostics as if they were a completed exhaustive run when the engine says discovery is blocked/unavailable/missing-inputs.

## NON-GOALS / PROTECTED BEHAVIOR

- Do not change `futureDiscoveryInputs()` filtering or enumeration.
- Do not change waiver legality, rankings, priority bands, projections, thresholds, IR behavior, lineup optimization, scenario scoring, or recommendation policy.
- Do not add a hidden candidate cap or shortlist.
- Do not change ESPN/provider normalization or source authority.
- Do not change `config/field-validation.json`.
- Do not mark FV-WAIVER-01 passed from automated tests alone.
- No package/version bump unless the established repository release process strictly requires one for a product UI change.

## TEST REQUIREMENTS

Add deterministic regression coverage proving:

1. the Waivers priority UI references and visibly exposes all four diagnostics when discovery is ready;
2. the UI continues to preserve blocked/unavailable discovery reasons rather than replacing them with misleading zeros;
3. no hidden weighted waiver score or candidate cap is introduced;
4. the existing waiver/domain test suite remains green.

Run the normal repository verification suite and obtain exact-head PR CI.

## FIELD RETEST GATE

After Manager integration and verified deployment, the user will:

1. Refresh ESPN against the real authenticated league;
2. open Waivers;
3. capture the visible diagnostic values and page responsiveness in one privacy-safe recording;
4. provide that evidence for Manager/Auditor review.

Only that deployed real evidence may support a later Manager-owned `FV-WAIVER-01` status integration.

## EXPECTED BUILDER HANDOFF

Return:

- exact branch and commit SHA;
- PR number;
- changed files;
- verification matrix;
- confirmation that waiver enumeration/policy logic was not changed;
- exact visible diagnostic wording;
- exact-head CI result;
- explicit statement that FV-WAIVER-01 remains pending until deployed field retest.
