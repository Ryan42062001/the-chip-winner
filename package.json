{
  "name": "the-chip-winner",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "start": "node scripts/dev-server.js",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/domain/recommendations.js && node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
